{
  "name": "gears-web",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:wasm": "../tools/build_wasm.sh src/wasm",
    "dev": "npm run build:wasm && vite",
    "build": "npm run build:wasm && vite build",
    "preview": "vite preview",
    "check": "npm run build:wasm && svelte-check --tsconfig ./tsconfig.json"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.0.0",
    "@tsconfig/svelte": "^5.0.8",
    "svelte": "^5.0.0",
    "svelte-check": "^4.0.0",
    "typescript": "^5.6.0",
    "vite": "^8.0.10"
  }
}
