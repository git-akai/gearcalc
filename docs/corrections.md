# Corrections

Every entry here was a claim this project believed and acted on, and every one
turned out to be wrong. They are recorded rather than quietly edited away,
because **the pattern is more useful than any single entry**:

> The errors that survived longest were the ones that looked reasonable and were
> never checked against something independent.

Two entries are corrections *of corrections* — the second diagnosis was right
about the symptom and wrong about the cause, and both times the wrong cause
looked like caution.

What the tool computes is [`reference.md`](reference.md); why each model was
chosen is [`rationale.md`](rationale.md); what is built is
[`state.md`](state.md).

---

## The patterns, and what now enforces them

Sorted by how often they recur. Each is followed by what makes it
unrepresentable rather than merely known — an entry earns its full length below
only while the fault it records can still be written.

### One idea written down twice

**A duplicated formula is a place where two answers can differ, and the copy
nothing exercises is the one that is wrong.** The internal relative curvature was
wrong two ways at once and unreachable; the gear tab's cutter default drifted
between Rust and TypeScript; the fillet's curvature was differentiated
numerically in two files; `MeshKind::sign` was written out again three times.

*Now:* the mesh kind is a signed number rather than a branch; defaults, bounds
and strings cross from Rust; the TypeScript wire types are generated and CI
regenerates them and requires no diff; the trochoid curvature is one closed-form
function shared by both cutters; and the tool an external gear is cut by is a
`Rack` of two **lengths**, because a coefficient converted on the way in and out
picked up a stray `1/cos β` every time it made the round trip.

### Work nobody reads is still work

**A search evaluates a candidate hundreds of times, so anything built per
candidate and then not read is the whole of what it costs.** Solving an
amplitude from a centre-distance offset built a *gear* per trial — every
distinct tooth generated — to read one shift off each: 102 ms, of which the
profile it was built for was 7.8 µs. Building any tooth ran a two-thousand-point
scan of its trochoid looking for a flank that undercut had removed, on teeth
that were not undercut and had nothing for it to find: 62 µs a tooth, and every
stage's shift search is hundreds of teeth. A hula stage's objective cut
each ring twice, once to check the tips and once to mesh them.

None of it was wrong, and no answer changed when it went — which is why nothing
caught it. What finds it is asking what a result actually reads, and the honest
way to ask is to measure the parts rather than the whole: each time, one line of
the breakdown was ninety-nine per cent of the total and the thing it produced
was never touched.

### A conditional's reason is narrower than what it guards

**Every guard is written for one thing and then covers everything inside it**,
and the gap between the two is invisible until someone reads both. Four
instances, all in one pass and all the same shape:

- `{#if g}` in a gear card meant *this rating needs an answer*. Inside it were
  the material override boxes, the field notes and every readout's **label** —
  so a stage that would not build withheld the very controls a designer would
  reach for to make it build. The material figures it hid are the library's, not
  the solve's.
- The `extra` hook's own comment said it was for a member with *no* shared
  readout. It rendered unconditionally, so the planet — which has one — printed
  its speed twice and hung the annotation on the copy.
- `min_tip_width` was read while resolving an automatic addendum, so an addendum
  typed by hand went unbounded and a tooth could come to a point in silence. The
  undercut floor had the same shape before it.
- A ring's `profile_shift.auto` and a hula stage's `given_shift` were read
  in one branch and ignored in the other: live controls that did nothing.

What they have in common is a flag or a guard whose *reason* covers less than its
*scope*. The question that finds them is not "is this condition right?" but "is
everything inside it conditional on that?" — and the fix each time was to name
the two things separately, which is what `auto` versus `no undercut`, and a
source versus a constraint, now are.

### A search may not re-judge the number it was handed

**A given value is a constraint on a search, not a candidate of it.** The shift
optimiser applied its floor to pinned gears as well as free ones, so a 43-tooth
wheel legally pinned at −0.5 — nowhere near undercut, and a whole module below
the chooser's `max(x_min, 0)` — made every candidate built on it inadmissible.
The search then found nothing, fell back to doing nothing, and the free gear
beside it came back at its own floor looking like an answer. Pinning a gear at
*exactly* its undercut minimum did the same thing, since that is the edge the
`undercut` flag is decided at.

There is no single right bound, which is why writing one was the mistake: a
shift arrives three ways and each earns a different answer to the same question
(`train::undercut_bound`). A search is floored where a chooser should not thin a
tooth for nothing; a given number carries no bound, having been held to one when
it was read; and a value a relation left carries the true minimum, because
nothing can move it and clamping it would break the relation that produced it.

### A check built from the thing under test measures nothing

The ring cut simulation derived the cutter's tooth the same way the model did, so
it agreed to 2.7 µm on a ring whose cutter was 0.44 mm out of place. The
mesh-phase coefficient's acceptance gate was "reproduce the backlash law", and
the answer turned out to *be* the backlash law halved.

*Ask what the check would say in the failing case — and if you cannot make it
fail, it is not a check.* Before trusting a new gate, run it against the broken
code.

### A reader that repairs is not a check

The sibling of the one above, and harder to see. `ezdxf` was chosen to read the
exported DXF back precisely because it shares no code with the writer — and it
*builds a document*: every structure the file left out, it supplied from its own
template and then pronounced the result sound. The export shipped with no
`BLOCKS` section, no `BLOCK_RECORD` table and no `OBJECTS` section, entities
owned by nothing, and passed every check for months. SOLIDWORKS repairs nothing
and refused the file outright.

*A tool forgiving enough to be worth checking against may be forgiving enough to
hide the fault. Ask what it does with a defect, not whether it opens the file.*
The structure is now read from the raw tags before any parser sees them.

### An axis nobody turns is an axis nobody tests

Every continuity check on the eccentric gear ran at λ = 0, which is the one value
at which the teeth *are* evenly seated. Then λ was swept on a lone gear — and
every `centre_profile` test, the place λ meets a *mate*, still left it at its
default. Three test grids between them left six of eleven inputs at their
defaults, and the module was 1.0 in every profile law the crate had.

*Ask of a control not "is it tested?" but "is it turned in each context it
reaches?"*

*Now:* one shared grid with every axis nameable, and module homogeneity asserted
as a standing law.

### A bound records where the sweep stopped

**A threshold taken from a measurement is a record of the parameters that were
swept, not a statement about the thing.** A guard was justified by sweeping
thirty thousand teeth and asserting the margin stayed above a thousandth of a
radian. Widening the sweep by one parameter — the root round, which had been
left at its default — walked the margin to 6e-4 on geometry that is perfectly
sound, and would have failed the gate.

The margin was fine; the bound was fiction. The reason the guard holds is that
the tool's tip round is capped at 95 % of the round that would fill the space
between the flanks, so the fillets are held apart by the five per cent and the
margin is a *fraction of the space* — it narrows with the tooth and never
closes. That says what to assert: that it stays positive. A bound has to be the
one the reasoning gives, or the next parameter someone adds to the sweep breaks
it.

### A gate on a ratio cannot see a scale error

`moment_per_force` returned a power where a torque was wanted, putting a factor
`z₂/z₁` — 40× on a worm — into every flank load. All four efficiency tests stayed
green, because efficiency is a ratio and the factor cancelled.

*If every gate on a quantity divides it by something, none of them constrains its
size.*

### Refusing to answer can be the discontinuity

`Y_S` was declined on a flank tangency to avoid a 17 % jump. But a number
becoming no number is also a jump, and nothing physical happens at 151 teeth. The
admissible shift ceiling stepped by 62 % as the eccentricity left zero, because
two arms of one branch had two different tool models.

*A value becoming a different value across a boundary the physics does not have
is the defect, whichever direction it goes.*

### A sentence cannot do a symbol's job

Four places matched on a message's *text* to decide something. Every one would
have gone quietly false on a rewording, and all of them on a translation.

*Now:* `Note::is(key)`, checked by the compiler, and errors carry a `Note` too.

### An absent thing is not a zero-length thing

The missing ring fillet was stored as `s_j = s_root = 0`, so everything
downstream asked for a curve that was not there — and the failure was silent and
*plausible*: a `NaN` arc length collapsed a 600-point outline to seven, which
draws as a polygon that looks deliberate.

*When something can be absent, say so in the type, or every consumer has to
remember.*

### Assembling a thing out of things gives each piece its own everything

An eccentric gear is built tooth by tooth from `Gear`s, and `Tooth::new` takes
gear-level decisions. Sorted by *whose property is this?*, the guards fall out
cleanly — but the sort was applied to two of the three, and the third came back a
week later wearing a different symptom. **The summary is one of the pieces** too:
`Gear::mean` kept the raw dedendum while the teeth were rebuilt deeper.

*When a construction is per-piece, list what is a property of the **whole** and
check each is actually shared.*

### An audit scoped by a construction reports silence as agreement

The crossed-axis audit asked "where is the contact path missing?" and found five
branches. Backlash does not use a contact path, so nothing in the sweep pointed
at it — and it was the branch actually carrying a bug.

*Scope a sweep by the **output** instead: every number a stage reports, and where
each one comes from.*

### Deriving a law to explain a refusal is how you get two bugs

A close tooth-count internal pair genuinely cannot absorb much eccentricity.
Inventing a plausible reading of λ to make the refusal go away kept the refusal
*and* broke the working case.

*When a model refuses, first check whether it is right to.*

### Group the cancellation first

`(seat + ψ) − ideal` against `(seat − ideal) + ψ`; a Fourier coefficient
projected onto raw samples rather than onto their deviation from the mean; a mean
summed outright rather than anchored on the first sample. Each cost a
*concentric* gear a nonzero answer where the true value is exactly zero.

*When a quantity must vanish in a degenerate case, arrange the arithmetic so the
terms cancel before they meet anything large, and assert the exact zero — the
tolerance is what hides it.*

### A disclosure can expire, and a stale one is a false statement

"This efficiency omits profile sliding" became wrong of the new model and then
fired on the *more* exact number. "The wheel is throated" was true of worm stages
and false of this crate, and was used to withhold a number the cylindrical model
produces perfectly well.

*Inherited justifications need re-reading against the code each time they are
leaned on.*

### A test that never meets the case it is about passes against the fault

A planet's root is rated over two meshes and takes the worse. The test written
for that had two fixtures and the sun mesh governed in both, so it passed
unchanged against the code that looked at one mesh — the thing it was written to
condemn. A third fixture with a narrow ring puts the other mesh in charge, and
then the old code fails by 2.7×.

The same shape twice more in one session: a sweep asserting that a stage's two
modules want to be equal had been comparing one drive that transmits against four
that do not; and a gate on "every number that crosses is a number" found its
first real absence only because it walked the whole result rather than the fields
someone thought to check.

*A test for "the worse of two" needs a case each way, and a test for "all of
them" has to enumerate rather than sample. Where that is hard to arrange, break
the code and watch the test fail — the only direct evidence that it discriminates
at all.*

### A `null` that crosses is not always a `None`

`serde_json` writes an infinity and a NaN as `null`, which is indistinguishable
from a field that honestly has no value and draws as the same blank. A face width
that resolved to zero made every stress infinite and every minimum a NaN, and the
whole row reached the screen looking exactly like a row of figures that were
never available — so the browser was **honest by accident**, while the CLI
printed `inf` and the generated TypeScript said `number` of a field that could
arrive `null`.

*Ask what each field can be rather than what it usually is, and let the type say
it. The gate is a walk over the whole result with an allowance per field name,
each carrying its reason.*

### A figure that changed is not evidence that today's change moved it

Three documented tables were found stale in one session, and the first was
diagnosed as a regression from the change in hand — until the previous commit was
built in a `git worktree` and printed the identical output. It had been stale for
some time, and the change under suspicion was innocent.

*Before attributing a moved figure to the diff in front of you, run the old code.
It costs a worktree and a build, and it is the difference between fixing a fault
and rewriting a correct answer.*

### Units are a diagnosis

A radial-assembly threshold came out scaling with ring *size* when a
tooth-passing condition must depend mainly on the tooth *difference*. A result
whose units are wrong is wrong however plausible.

---

## The log

<!-- figures-exempt: a record of what this project believed and got wrong; these figures are history and must not be regenerated -->

| Where | What was wrong | How it surfaced |
|---|---|---|
| [4.3](reference.md#automatic-values) | Working depth modelled as a constraint on the form radius | Would not reproduce the classical z=17 result; the correct reading substitutes `h_w` for `h_f` in the cutter-depth term |
| [4.5](reference.md#path-of-contact-and-contact-ratio) | Approach and recess lengths each subtracted `a_w sin α_w` | Both came out **negative**; each must subtract its own gear's `r′ sin α_w`, and only their sum uses `a_w` |
| [4.6.1](reference.md#tolerance-tables) | "The standard scale's grade 4 is tighter than the fine scale's grade 0" | False — 7 against 6.3. The argument it supported (non-monotonic merged ladder) survives and is now stated from the transcribed data |
| [4.6.1](reference.md#tolerance-tables) | JGMA diameter bands stored half-open as printed | Left a **gap**: a gear of exactly 12.00 mm got no tolerance at all |
| [4.7](reference.md#contact-stress) | 30° tangent treated as the model rather than an approximation | Its tangents miss the load point by 11.8% at z=9; the Lewis parabola is the construction the cantilever model implies |
| [4.7](reference.md#contact-stress) | Parabola tangency searched on the fillet only | No solution at all above z≈150 — on large teeth it touches the **flank** |
| [4.7](reference.md#contact-stress) | ISO `Y_S` applied to a flank tangency, using the **involute's** curvature as `ρ_F` | **17% discontinuity** at z=150→151 while `Y_F` moved 0.03%. `ρ_F` is a notch radius and an involute is not a notch: the number jumps 0.61 → 22.9 mm across one tooth |
| [4.7](reference.md#contact-stress) | ...then refusing the correction there instead | **Also a discontinuity** — a number becoming no number — and the diagnosis had stopped one step short. Nothing physical happens at 151 teeth, so nothing may step. The notch is the *fillet*, and when the critical section climbs above it the nearest fillet point is the junction; read there, `ρ_F` runs smoothly through the seam (0.6095, 0.6081, 0.6067) and `q_s` with it (1.805, 1.808, 1.808). **This mattered far more than the external case suggested**: a ring's tooth is short and wide, so its section sits on the flank for *every* count from 40 up — the old rule withheld ring bending across most of the design space, appearing and disappearing as the contact ratio moved the load point |
| [4.7](reference.md#contact-stress) | "Rack-generated fillets keep `q_s` in range" | False at large z — 10.3 at z=300 with a sharp cutter |
| [4.5, 4.7](reference.md#contact-stress) | "A helical mesh slides along its contact line, and the efficiency formula under-states that loss" | **No such component exists for parallel axes.** Building the sliding as a vector and resolving it on the contact line returned zero at every helix angle to 1e-14 of the pitch line velocity. Both surface velocities are `ω ẑ × r`, so the sliding is transverse, while the contact line is not — and the two are orthogonal by construction. The closed form was already exact; two documents described it as conservative |
| [4.5](reference.md#path-of-contact-and-contact-ratio) | Mesh efficiency without the `/ε_α` | Implicitly let every engaged pair carry the full load, so the mesh transmitted `ε_α F_n` and the loss came out too large by exactly the contact ratio. Caught by a numerical average of the instantaneous loss |
| [4.7](reference.md#contact-stress) | Hertz checked at "the inner point of single-pair contact (usually the pinion's worst case)" | Label-dependent: the relative-radius peak moves to the other side when gear 1 is the wheel, so one physical mesh gave two answers. Both boundaries are now checked |
| [4.7](reference.md#contact-stress) | `Load` stored a force in a field named `normal_force` | The value was the **transverse** `F_bt = T/r_b` while the name asserted the normal plane. Numerically right for spur, but the name would have survived a refactor its meaning did not. Now stores torque, with each projection named at its point of use |
| [4.7](reference.md#contact-stress) | Helical contact used the transverse force, face width and curvature throughout | Three separate `cos β_b` factors were missing. They nearly cancel — the net is `√(cos β_b)` — so the error was small but the model was wrong in three places at once rather than right |
| [4.7](reference.md#contact-stress) | Helical bending measured `Y_F` on the transverse section and divided by `m_n` | Mixes planes; under-predicts by about `cos β` (6 % at 20°, 13 % at 30°). Now measured on the ISO virtual spur gear `z_n = z/cos³β` |
| [4.5.1](reference.md#crossed-axes) | "Four lines are tangent to both base cylinders; **only one** passes through the pitch point" | Eight, and **two** pass — the tooth's two flanks, drive and coast. The count came from enumerating one normal direction and forgetting the other, and it was hidden because the two are mirror images, so every *length* the path yields is the same on either. It surfaced only when the scratch script was promoted to `tools/crossed_path.py` and the tangency check was added: floating-point noise was deciding which of the two flanks was returned, and the flank normals then came out 83° from it |
| [4.5.1](reference.md#crossed-axes) | The flank tangency check compared the crate's normal against one flank of each member | A tooth has two, and in mesh the driving flank of one member meets the **facing** flank of the other. Pairing them wrongly puts the answer out by exactly `2α_n` — 40° here — which looks like a broken construction and is a broken comparison. Both faults were in the *checks*, not in the crate, and neither could have been found while the scripts lived in a temporary directory |
| [4.4](reference.md#centre-distance-and-backlash) | A crossed pair's centre-distance backlash as `Δa · sin α_n` | **Half the answer.** A separation opens *both* flanks and lost motion is the sum of the gaps a member can travel across, so the term is `2 Δa sin α_n`. It reached the UI as a **50 % step** in backlash the moment the shaft angle left zero — the parallel stage had the exact involute law, the crossed stage had half of its first-order form, and moving `Σ` from 0 to anything crossed between them |
| [4.4](reference.md#centre-distance-and-backlash) | ...and the reason it survived | The module derivation wrote both displacements as one gap along `n̂` — `j_n = j_axial sin β_b1 + Δa sin α_n` — which reads as a single tidy projection and is two different physical quantities. A worm's axial float is a **rigid-body slide** (opens one flank as far as it closes the other: counted once); a centre-distance error is a **separation** (opens both: counted twice). One coefficient for both hid the factor. The test that existed, `axial_slack_reproduces_the_two_handbook_relations`, deliberately zeroed the clearance to isolate the axial term, so it constrained the half that was right |
| [4.5.1](reference.md#crossed-axes) | "Unifying backlash is not obviously a gain... expressing the parallel case in the crossed form would trade exactness for uniformity" | True of the **numerators** and false of everything else, and acting on it as though it settled the question is what left the error unguarded for a milestone. The *conversion* from gap to angle was one law with two homes, and the second home had no check against the first. Every crossed backlash test held the crossed model against itself; the parallel model — which has the exact law and its own tests — was never asked |
| [4.4](reference.md#centre-distance-and-backlash) | Every contact quantity rated at the **zero-backlash** centre distance | A pair runs at that plus its assembly clearance, and the path, the operating pressure angle, the curvature and every stress belong to where it runs. `ε` was over by 1.2 % and bending under by the same, on every parallel stage ever solved. The clearance was being treated as a tolerance to ignore when it is the reason the stage reports backlash at all |
| [4.5.1](reference.md#crossed-axes) | `path_of_contact` returning `None` at any centre distance but the nominal | Not a refusal, a **missing branch rule**: it identified the mesh by asking which tangent line passes through the pitch point, and none does once the centres move. Which flanks face each other is not a function of centre distance, so the branch is settled at the nominal distance and carried |
| [4.5.1](reference.md#crossed-axes) | A crossed pair's face width clipped symmetrically about the path's own origin | The face is centred on its **gear**. Those coincide only at the zero-backlash distance; away from it the contact has slid along the shafts, and at `Σ = 0.5°` with 20 µm of clearance a 12 mm face has `ε = 0.860` where the model reported 1.664 |
| [11.5](rationale.md#no-english-in-gear-core-and-no-engineering-in-the-catalogue) | Notes as `Vec<String>`, and code matching on their text | Four places branched on a *sentence* — the planetary solve on `"tip radius raised"`, the range test on `"cutter depth"`, two ring tests on `"base circle"` — every one of which would go quietly false on a rewording and all of them on a translation. A note that anything acts on needs an identity, not prose |
| [11.5](rationale.md#no-english-in-gear-core-and-no-engineering-in-the-catalogue) | The string catalogue read from a plain module variable in the front end | Not reactive, so the **sidebar** — which draws before the core finishes loading — kept the fallback and rendered `ui.sidebar_gears` where "Gears" belonged. Every panel looked right because every panel renders later, which is exactly how a bug survives a look at the screen |
| [4.3](reference.md#automatic-values) | `working_depth` defaulting to a fixed 1 module | The control exists to expose the classical rule's hidden assumption, and it shipped defaulting *to* that assumption. It follows the gear's own dedendum now, which is the depth the `undercut` flag answers about — the two agree by construction instead of by coincidence. Every automatic shift previously clamped at zero moved, and nothing in the suite noticed |
| [4.5.1](reference.md#crossed-axes) | "What a crossed stage lacks is the *backlash from thinning* one alone, which would need the normal-plane play derived for crossed axes" | Recorded as a gap; it is an **answer**. The play is derived ([centre distance](reference.md#centre-distance-and-backlash)), and `k₁ + k₂ = 2` means thickness is only ever moved between two teeth and never removed from the pair, so what it contributes is exactly zero. A missing derivation and a derivation that returns zero read the same in a document and are not the same thing |
| [11.4](rationale.md#two-friction-coefficients-because-there-are-two-questions) | One friction coefficient per mesh | Breaking away and running are different events with different coefficients. Every efficiency in the model was the sliding one, so a worm that cannot be started was reported as back-driving at 54 % — the efficiency of a motion that does not happen |
| [11.4](rationale.md#two-friction-coefficients-because-there-are-two-questions) | The self-locking note quoting the sliding coefficient | It named the input a reader would go and change to no effect. Whether the pair locks is decided by the **static** coefficient, and the note now says which number it means |
| [4.10](reference.md#angularly-varying-profile-shift) | Each tooth of an eccentric gear built as its own gear, tool and all | `Tooth::new` caps the cutter tip round per tooth, so the high side got a 0.2375-module tool and the low side 0.3800 — a different hob for each tooth. The fillet collapsed on the high teeth and the trochoid's extent jumped sixfold between neighbours. One gear, one tool: the tightest tooth's |
| [4.10](reference.md#angularly-varying-profile-shift) | ...and each tooth's root drawn at its own radius | A radial **step at every mid-space**, up to 0.13 mm, which no hob can leave. The root is the gear's, not the tooth's: it runs from the fillet junction to the envelope `r − m(h_f − x(θ))`, continuous at both ends |
| [4.10](reference.md#angularly-varying-profile-shift) | The root correction applied to the flat root alone | It has 0.005 rad to absorb 0.05 mm in, a dive of 9 mm/rad against the envelope's own 0.4 — a notch at the bottom of every tooth space. Spread across the fillet, which the same tool corner cuts at the same moving radius and which nothing constrains |
| [4.10](reference.md#angularly-varying-profile-shift) | ...and parametrised on **angle** | The flank is re-entrant below the base circle, so a flank point can sit at a larger angle than the fillet junction and take a displacement it must never have. Radius is the monotone invariant ([the generated profile](reference.md#the-generated-profile)) and is what measures position along the profile |
| [8.0](rationale.md#notes-must-not-move-the-controls) | A gear tab's type-specific inputs left set when the type changed | Switching an eccentric gear back to external left its shift amplitude in place, so the gear stayed eccentric with no control on screen to say so — and the eccentricity outputs keyed on that *value* rather than on the type, so they stayed too. Changing type now returns every field the new type does not use to its default, read from `FIELDS` rather than from a second list |
| [4.10](reference.md#angularly-varying-profile-shift) | Only the cutter *tip round* shared across the teeth | The **depth** is a tool setting too. `Tooth::new` raises the cutter depth when it would go non-positive, which pinned four teeth to one root radius while their neighbours followed the envelope — a flat spot and a corner on the high side at positive shift, and the low side at negative. Both settings are the tool's and are settled once; what is a fact about *one tooth* is reported instead |
| [4.10](reference.md#angularly-varying-profile-shift) | Each tooth drawn one **ideal** pitch wide | λ seats the teeth unevenly by construction — that is what it is for — so the space between two of them is a pitch plus the difference of their offsets. Every tooth drawn to the ideal width left a gap of 0.009 rad at λ = 1, and every continuity check written before it ran at λ = 0, the one value that hides it |
| [4.10](reference.md#angularly-varying-profile-shift) | The shared cutter depth applied to the teeth but not to `Gear::mean` | When a high-shift tooth forces the cutter deeper than the dedendum asked for, the teeth are rebuilt to it — but `mean`, which every scalar is quoted from and which `root_at` builds the root envelope on, kept the raw dedendum. The drawn root then stood `m·(depth − dedendum)` proud of the teeth and protruded past the fillets: **0.25 mm** on `α=25° z=23 x=0.2 Δx=1 addendum=0.8 dedendum=1.0`, plainly visible on the shallow high side. Every step/kink trend test passed — a uniform radial offset of the whole root is smooth. `mean` is rebuilt with the same tool now, in lockstep with the teeth so a concentric gear is still `Tooth::new` verbatim; the gate is `root_at(seat_k) == teeth[k].rf` at λ = 0, exact. *The object that summarises the pieces is a piece.* |
| [4.10, 8](reference.md#angularly-varying-profile-shift) | The wasm gear summary built from `Gear::new(params)`, ignoring the eccentricity and the shared-tool rebuild | Same root cause on a second surface: `fillet_radius` reported the 0.38-module rack where the shared tool is 0.05, `cutter_tip_width` used the raw dedendum, and span/over-pins quoted the mean tooth as if it were *the* measurement. `solve_gear` now builds `Gear::new(params)` and quotes `mean()` — one construction for both kinds, a concentric gear's `mean` being `Tooth::new` bit for bit. Span and over-pins are withheld for an eccentric gear (`Unavailable`, "varies around the revolution"); `undercut`/`severed` became "any tooth" (`per_tooth_clamps` names which); the panel hides the tip/root/thickness scalars for an eccentric gear and shows the `variation` ranges only |
| [4.10, 8](reference.md#angularly-varying-profile-shift) | `admissible_ranges` bounded the nominal tooth, not the swept interval | An eccentric gear's teeth are cut across `x̄ + Δx cos θ`, so `x̄ = 0.2, Δx = 1` puts a tooth at 1.2 — past the buildable 0.95 — while the hint said the shift was fine and the root-radius hint offered the 0.38 rack the high teeth cannot hold. `admissible_ranges` now closes the shift-dependent bounds onto the interval: `profile_shift` pulls in by each extreme tooth's own offset (its cutter-depth ceiling drops — the shared tool follows the shift up), and `addendum`/`dedendum`/`root_radius` are the tighter of their two extremes against the shared cutter depth. Gated against the generator as the concentric bounds are — just inside the window every tooth builds, just outside one clamps. A concentric gear is `Δx = 0` and unchanged to the bit |
| [4.10](reference.md#angularly-varying-profile-shift) | `centre_profile` reported `CentreDistanceTooSmall` where the cause is `inv α_w < 0` | Two different failures wore one name. `operating_geometry` returning `None` means no operating pressure angle exists — the tooth is too thick to sit in the mate's space at *any* centre distance — which is `OutsideInvoluteDomain`; `CentreDistanceTooSmall` is the base-circle limit and belongs to the sinusoid-fit step below it. On a close tooth-count **internal** mate the first fires readily, because the shift term carries `1/Σz` and `Σz` is the tooth-count *difference* there, so a modest amplitude is amplified into it. The message now names the eccentricity and says what relieves it |
| [4.10](reference.md#angularly-varying-profile-shift) | ...and the "fix" for it invented a law, which broke the feature outright | Diagnosing the above, `x_eff = x̄ + (1 − λ)(x_k − x̄)` was introduced on the reasoning that a corrected drive flank sits where the mean tooth's would, so a gear at λ ≈ 1 operates against a smaller shift interval. **λ moves a tooth rigidly** — both flanks by one angle — so it decides when a tooth arrives, not how thick it is, and zero backlash is set by the thickness. The consequence was not subtle: at λ = 1 every tooth collapsed onto the mean and the commanded centre distance reported **zero throw at every amplitude**, which is the whole output of the feature. Reverted; λ reaches nothing here and that is now gated *exactly*, over both mesh kinds and four λ. The gap that let it through: every test that turned λ did so on a lone gear, and every test with a mate attached left λ at its default — **a control needs a case that turns it in each context it reaches**, not just somewhere |
| [4.10](reference.md#angularly-varying-profile-shift) | `centre_profile` answered an internal pair whose "ring" had fewer teeth than the pinion | `Mesh::new` refuses it as `RingTooSmall`; this arrived only as a signed sum of the wrong sign and the arithmetic answered it. Refused by name now |
| [6](rationale.md#material-data-ships-estimates-deliberately) | Two-point Basquin S-N law per material | The data does not exist — no polyamide grade publishes any fatigue figure, and POM's is a printed graph. Replaced by peak and cyclic allowables, [material data](rationale.md#material-data-ships-estimates-deliberately) |
| [6](rationale.md#material-data-ships-estimates-deliberately) | `yield_strength` as the single strength field | Glass-filled grades have **no yield point**; their datasheets report stress at break. Renamed to an allowable, with `ultimate_measure` recording which quantity it is |
| [6](rationale.md#material-data-ships-estimates-deliberately) | "1215 Hardened Steel" assumed a valid entry | 1215 is ~0.09 %C and cannot be through-harden; only carburised, giving a hard case over a soft core that one scalar cannot represent. Both 1215 entries dropped |
| [6](rationale.md#material-data-ships-estimates-deliberately) | Delrin 570 assumed a reasonable "POM GF20" | It is glass *filled*, not *reinforced* — **25 % weaker** than unfilled Delrin. Entry dropped; only a glass *coupled* grade would belong |
| [6](rationale.md#material-data-ships-estimates-deliberately) | PA6/PA66 stiffness "roughly halves" when conditioned | Understated: unfilled PA6 modulus falls 3000 → 1000 MPa, a factor of **three** |
| [4.10](reference.md#angularly-varying-profile-shift) | Read as an axial taper (beveloid) | It is an *angular* variation; the beveloid treatment was withdrawn entirely |
| [4.10](reference.md#angularly-varying-profile-shift) | "No changes to the generator" (beveloid reading) | Did not survive the correction above |
| — | Involute inversion by series seed + Newton | **Diverges above ~60°**, inside the allowed pressure-angle range; needs safeguarding |
| [4.7, 4.11](reference.md#internal-gears) | Internal relative curvature written as its own branch beside the external one | **Wrong in two independent ways at once**, neither reachable. `r_b2` was scaled by `z₁ + z₂` where an internal pair needs `z₂ − z₁` (exactly 0.5× on a 17/51 pair), and `ρ₂ = r_b2 tan α_w − ξ` should be `+ ξ`. Together: −50 % at the pitch point of a 17/51, and a **negative** relative curvature on a 25/41 — which `contact_stress` would have reported as "no contact" for an ordinary internal mesh. Both were dead code only because `ContactPath::new` admitted no internal mesh, and both would have gone live with milestone 9. Fixed by making the sign a *value* rather than a branch: gear 2's tooth count, shift and radii are negative for a ring, and the external expressions then serve both kinds unchanged |
| [4.7](reference.md#contact-stress) | The two members' base radii reached by different routes | Gear 1's came from its own reference geometry, gear 2's through `a_w` and `α_w` — so `r_b1 + r_b2 = a_w cos α_w` held only to an ulp, and gear 2's carried the involute inversion's residual for no reason. A base radius is `m_t z / 2 · cos α_t` and owes nothing to the centre distance; both now take that route, and the identity is exact |
| [4.11](reference.md#internal-gears) | `thickness_mod` applied to a ring's **tooth**, as it is to an external gear's | Backwards. The ring's *space* is what is generated like a tooth — it is where the pinion's tooth goes — and `Mesh::new`'s internal relation flips gear 2's `x` and `x_s` together, which is consistent only with the space reading. Measured against tooth thicknesses at the operating circles, the space reading gives exactly zero backlash at every k while the tooth reading is **0.63 mm** out at k = 1.2. Consequence: a larger k or x makes a ring's tooth *thinner*, and the internal pair invariant is `k₁ = k₂` where an external one needs `k₁ + k₂ = 2` |
| [4.11](reference.md#internal-gears) | `Ring::new` ignored `profile_shift` entirely | Not a wrong formula but a missing one, and it had a visible face: the gear tab sends `profile_shift` for an internal gear, so the box was there and moved nothing. Every layer was individually happy, which is why only an end-to-end check found it |
| [4.11](reference.md#internal-gears) | A shifted ring cut by a cutter at **reference** centres | A shaper cannot be displaced the way a rack can. A rack's pitch line is a machine setting, so shifting it leaves the rolling alone; two pinions have their ratio fixed by their tooth counts, so the pitch point is wherever the centre distance puts it and the rolling circles move with it. The cutter was up to **0.44 mm** out of place at x = 0.5. One factor `a / a_ref` carries all of it, and is exactly 1 at zero shift |
| [4.11](reference.md#internal-gears) | **The cut simulation derived the cutter's tooth from the ring's** | So it shared the model's assumption and could not see the fault above: it reported 2.7 µm on a ring whose cutter was 0.44 mm out of place, unchanged from the unshifted case. The this log trap in its purest form — *a check built from the thing under test measures nothing.* The cutter's tooth now comes from the cutter, and placing it at reference centres makes the gate fail by 13–66× its noise floor |
| [4.11](reference.md#internal-gears) | A ring's root radius from `r + m(dedendum + x)` | That is the exact relation linearised. A ring's root is wherever its cutter's tip *reaches*, `a_cut + r_tip`; the two differ by 17 µm at x = 0.25 and 57 µm at x = 0.5, both well above the 3.6 µm the cut simulation resolves. So a ring has **no dedendum input** — it is the cutter's addendum seen from the other side, and having both invites them to disagree. Its root-radius coefficient goes the same way: the fillet round is the cutter's own |
| [4.11](reference.md#internal-gears) | `Ring::new` accepted a shaper **at least as large as the ring** | It arrives as a negative centre distance rather than an obvious error, so nothing objected: a 43-tooth ring "cut" by a 50-tooth shaper reported a root radius of 29.75 mm against a pitch radius of 21.5, and the only complaint was about the tip corner — a true statement of the wrong problem. `Mesh::new` and `mesh_with` both refuse that pairing; the constructor did not. Now clamped and named |
| [4.11](reference.md#internal-gears) | A ring's tooth was never checked for **running out of thickness** | A ring's tooth narrows *inward*, so its tip is its thinnest section — and where `ψ_b < 0`, which needs `π/2z < inv α_t` (about 105 teeth at 20°), it reaches zero *above* the base circle. Unclamped that is not a thin tooth but a **crossed** one: a 150-tooth ring at a 3-module addendum came out at −0.211 mm of tip thickness, and its outline is a self-intersecting polygon bound for a DXF. The limit is closed form through `inv⁻¹` — `inv α = −ψ_b` — and mirrors `Gear`'s pointed-tooth clamp rather than being a new kind of guard. Invisible on the tooth counts one would try first |
| [4.8](reference.md#planetary-sets) | `mesh_with` asserted standard centres for an internal pair | Fine as far as it went, but a planetary set needs the shifted case and the two would then have been separate constructions. Now both come from `mesh::operating_geometry`, the same relation the external mesh uses, and a standard pair is its value at zero shift — reached rather than asserted, and equal to `r_ring − r_pinion` to 1e-12 |
| [4.8](reference.md#planetary-sets) | The planet-shift bracket evaluated **on** the involute domain's boundary | The endpoints are where `inv α_w = 0` exactly, and whether that arithmetic lands on zero or on −1e-17 depends on the tooth counts. On a 24/16 set with four planets the upper endpoint fell a hair outside a domain it was meant to sit on, so `z_ring = 57` was refused although its root is at +0.2485, comfortably inside — **a hole in a run that monotonicity says is contiguous**. Fixed by halving each endpoint toward a point known to be interior, which finds the last representable point in the domain and needs no tolerance: it stops when the answer becomes a number |
| [4.8](reference.md#planetary-sets) | Contiguity checked on one configuration | The test swept 17/17 only, where the rounding above happens to fall the other way, so it passed while a neighbouring set had a gap. The property is about *all* sets; it is now asserted over eight, and as a **gap is a bug by construction** it needs no knowledge of the answers |
| [4.5, 4.7](reference.md#contact-stress) | `ContactPath` external-only, with the internal case listed as "contact needs no new code" | Half right. `hertz::relative_curvatures` and `contact_stress`'s curvature genuinely needed nothing, but the **path** did: it is the input to the contact ratio, the efficiency, both single-pair stress boundaries and the bending load point, and it refused an internal mesh outright. It is now one pair of expressions for both kinds, with the tangent length carrying the sign of its own base radius — so a ring's tip sits at a *smaller* tangent length than the pitch point and its approach comes out `\ |
| [4.5](reference.md#path-of-contact-and-contact-ratio) | `ContactPath::new` took gear 2 as a whole `Gear` | It only ever read `r_a2`, and a ring's tip radius cannot come from a `Gear` — it is inside the pitch circle and set by the cutter. Now takes the tip radius, which is both less than it asked for before and enough for the internal case |
| [4.5](reference.md#path-of-contact-and-contact-ratio) | `sliding_at` placed gear 2 at `+a_w`, unreachable *because* no internal path existed | The comment saying so went stale the moment the path was lifted, which is the hazard of documenting an assumption as safe on the strength of something elsewhere refusing. A ring encloses its pinion rather than sitting beside it, so its axis is at `−a_w`; from the signed operating radii `r′₁ + r′₂` gives both, and the pitch point stays put |
| [4.7](reference.md#contact-stress) | "A ring's bending would need its own critical-section construction" | It needs its own *curves*, not its own construction. The inscribed parabola's tangency condition `X·Y′ + 2X′(y_v − Y) = 0` is **odd in y**, so negating `y`, `Y′` and `y_v` together leaves its zero set untouched — and the moment arm is a difference, so it flips with the frame and comes out positive either way. One flip is the whole of what an inward-pointing tooth needs, and external stays a value of the same search rather than a sibling of it |
| [4.7](reference.md#contact-stress) | Figure 8's "the two factors approach each other" taken as a check | It is a property of the paper's setup, not of ours: TM-107012 cuts its external gear with the same 20-tooth shaper as its ring, while this crate's external gear is rack-cut. With the shaper held at 20 teeth the ring's fillet never tends to the rack's, so the gap closes only to z ≈ 150 and then widens. The real convergence is with the shaper growing too, where both teeth become the same rack tooth |
| [4.7](reference.md#contact-stress) | Helical rings left unrated, on the grounds that a virtual spur *ring* did not exist | A gap, not a decision — the stated intent is feature parity with spur gears. It exists now: `z_n = z/cos³β` at the normal module, **with the cutter virtualised the same way**, which leaves `z_c/z_r` unchanged so the virtual pair still rolls together and the cut stays conjugate. Needed a `Ring` buildable at a fractional tooth count, mirroring `Gear::build_with_z`, and a `CutParams` that takes the cutter's *radius* rather than a whole-number tooth count |
| [4.9](reference.md#trains) | A planetary stage assumed to fit the train's two-member shape | It has three shafts, and its speeds come from its own kinematics rather than from a ratio applied to the previous stage. `solve_any` therefore needs a **speed** as well as a torque: which shaft is held is a kinematic question, and the efficiency depends on it. The train now carries the speed it has reached alongside the torque it has reached |
| [4.11, 3](rationale.md#no-engineering-calculation-in-typescript) | **The gear tab's cutter default was written down in TypeScript as well as in Rust, and the two drifted.** | `defaultCutter()` carried `tip_round = 0.38` — the *rack's* figure — where `Cutter::default()` has held 0.2 all along, with a comment saying that a 20-tooth shaper's tip is 0.377 modules wide and cannot hold two 0.38 rounds. So every ring the UI built was cut by a tool that generates no fillet, and the viewport drew a sharp-rooted polygon with straight flanks. Nothing in Rust was wrong; nothing in Rust could see it either, because the wrong number lived only on the side that has no tests. **The fix is not the number.** Every default a fresh tab starts at is now served across the boundary by `gear_wasm::defaults`, so there is one home for them and the class of drift is closed rather than this instance of it |
| [4.11](reference.md#internal-gears) | A ring with no fillet was represented as one whose fillet has **zero length** | `s_j = s_root = 0` and a `Trochoid` section still in the list. Sampling it measured an arc length of `NaN`, which made the total `NaN`, every share `NaN`, and `(NaN) as usize` **zero** — so every section fell back to its minimum point count and a 600-point outline came back with **seven**: an involute drawn as two straight chords. The absent fillet is now `Option<Fillet>` and cannot be asked for as a curve, the root arc starts wherever the section before it finished, and the allocator drops a section of zero or non-finite length instead of letting one poison the rest |
| [4.11](reference.md#internal-gears) | The boundary test that was supposed to prove "an outline the viewport can draw" | Was built on the same 0.38 cutter, and asserted `outline.len() > 200` — which sixty teeth of seven-point rubbish clears comfortably. *A check built from the thing under test measures nothing*, in its second form: the fixture was the defect. It now asserts points **per tooth** against the number requested, and the standing gate in `geometry_laws.rs` states the two properties the drawing must have — that the points arrive at the density asked for, and that each flank chord stands at `√(r² − r_b²)` from the centre, which is what makes the curve an involute of its own base circle. Run against the pre-fix code, that gate fails with "asked for 600 points a tooth and got 13.0" |
| [4.5.1](reference.md#crossed-axes) | "A worm stage reports no contact ratio — **the zone of action for a throated wheel** is not derived" | The phrase was true of worm gearing generally and false of this crate: [crossed axes](reference.md#crossed-axes) takes both flanks as involute helicoids on **cylinders**, and nothing here throats anything. It had been written when no zone of action was derived for any crossed geometry — true then for a simpler reason — and was later leaned on to withhold the one number the cylindrical construction produces, while every other number in the same result came from that model unquestioned. A worm reports it now, as a floor, with its assumed tooth height named |
| [4.5.1](reference.md#crossed-axes) | "The contact path unblocks crossed-axis bending" — the audit's own prediction | It gives the load's position along the *profile*. `σ_F = F_t/(b·m)·Y_F·Y_S` is a cantilever loaded across its whole *face*, and a crossed pair's load is a point. Two missing ingredients looked like one until the formula was read; choosing an effective width is a convention that multiplies a stress, so [contact stress](reference.md#contact-stress) refuses it and bending stays out with a reason rather than a gap |
| [4.5.1](reference.md#crossed-axes) | A **disclosure that outlived its premise**, and then accused the better number | While the crossed efficiency omitted the profile sliding, a law disclosed it: crossing shafts can only add sliding, so beating the same teeth run parallel meant sliding had gone missing. Once the friction balance counted that sliding, the comparison inverted — the *parallel closed form* is the approximate one, first order in `μ` where the balance is exact — and at Σ ≤ 0.5° the check fired on a figure that was a hundredth of a point **better**. Removed. A warning is a claim, and it expires with the thing it warned about |
| [4.5.1](reference.md#crossed-axes) | The friction balance's flank load, out by `z₂/z₁` — **forty on a worm** | `moment_per_force` returned a *power* where a *torque* was wanted. All four gates on the balance were on the **efficiency**, which is a ratio, so the factor cancelled and every one stayed green; on a stress it does not, and a cube root turned forty into a plausible-looking 3.4×. Caught by the canary, not by the suite. A gate on the load itself now compares it with the classical closed form, and re-breaking the code deliberately confirms it is the only test that fails |
| [4.5.1](reference.md#crossed-axes) | "A narrow face pushes the rating 10–27 % past the pitch point" | Measured at 1.2 %, not 27 %. Losing load sharing does push the rating outward, but a face narrow enough to lose it has also cut the zone's ends off — and those ends were what made the path severe — so the two effects nearly cancel. The 10–27 % figure describes the zone's extremes, which are never rated because the load is shared there |
| [4.5.1](reference.md#crossed-axes) | "Frictionless is lossless **to the last bit**" | To a few ulps. The answer is a ratio of two moments reached by different cancellations, so bit-exactness was never on offer; the assertion said more than the arithmetic could and failed on the first geometry that exercised it |
| [4.5.2](reference.md#planetary-sets) | Reversing a planetary by handing its output shaft's torque back as the input | That torque is a **reaction**, opposite in sign to the shaft's speed, and a shaft that is now driving must have them agree. The wrong sign flips the rolling power, `η₀^w` takes the wrong branch, and the set reports an efficiency **above one** — 101.571 % in the running application. Every test drove forward with a positive torque and a positive speed, so none could see it; found by looking at the UI. Related: a shaft with `T ω ≤ 0` is absorbing power and is not an input at all, so naming it one is now refused rather than answered with `1/η₀` |
| [reference.md#angularly-varying-profile-shift](reference.md#angularly-varying-profile-shift) | The shared tip round settled as a **coefficient** | Divided by the normal module and handed back as a `root_radius`, which is re-multiplied by `m/cos β` — so every rebuild inflated a helical gear's round by `1/cos β`, 1.22× at 35°, and the teeth came out cut by different tools. Invisible because every eccentric test used a spur gear. The tool is two lengths now, so there is no coefficient to round-trip |
| [reference.md#internal-gears](reference.md#internal-gears) | A tip round too large for a shaper's tip **refused** the tool | The same guard caps it on an external gear. So one input gave a fillet outside and *no fillet at all* inside — a jump in kind at a point where nothing physical happens. Capped now, by the same rule and with the same note |
| [reference.md#internal-gears](reference.md#internal-gears) | ...and the first cap was not monotone | Backing off by the 5 % margin only once the ask crossed the boundary dropped the realised round *at* that instant. `min(asked, 0.95 × max)` rather than `if asked > max { 0.95 × max }` — a clamp has to be continuous in what it clamps |

| [reference.md#metrology](reference.md#metrology) | A span and a pin measurement taken as a difference of two **accumulated** seats | `t(k+1)/z - tk/z` is an ulp or two off `t/z`, which gave an evenly cut gear a measurement that varied around a revolution it is constant on — and reached the screen as "11.258 to 11.258 around the revolution". Grouped on the pitch and the `psi` differences, which vanish exactly when the teeth agree. The third time this module has met *group the cancellation first* |
| [reference.md#metrology](reference.md#metrology) | ...and the pin index wrapped **before** the difference was taken | An offset of one space became one of `z - 1` — the same angle, and not the same `cos`. `Gear` wraps for its own lookups, so the caller must not |

| [reference.md#trains](reference.md#trains) | **Every rating judged against the fatigue allowable, including the peak one** | The crate has held `ultimate_allowable` and `fatigue_allowable` since the material library existed, and rated everything against the second. So a load the train has to survive *once* was being asked to survive it forever — a wrong question, asked with a confident number attached, failing gears that are fine. The two are separate cases now, and what distinguishes them is exactly the torque and the allowable |
| [reference.md#trains](reference.md#trains) | A planet's reversed-bending derate written down **twice** | `width_for` sized the planet's face against the reversed allowable while `gear_result` reported its minimum against the plain fatigue figure, and a separate `min_face_width_reversed` field carried the other answer beside it. Two numbers for one question, and the width the stage actually used matched neither label. `gear_result` takes the allowable it rates against, so the width a member is given and the minimum it reports come from one function; the extra field is gone |
| [rationale.md#a-load-exists-only-where-it-is-reacted](rationale.md#a-load-exists-only-where-it-is-reacted) | A back-driving load first modelled as a **sign on the input torque** | It enters at the far end and is attenuated by *backward* efficiencies, which are not the forward ones and can be zero. Worse, the interesting case is the one where the number is absent: a train nothing can hold does not carry the load at all. Reported per stage as an `Option`, with the train saying which stage reacted it or that none did |

| [rationale.md#one-pressure-two-ratings--and-the-curvature-is-not-what-separates-them](rationale.md#one-pressure-two-ratings--and-the-curvature-is-not-what-separates-them) | **Both gears of a mesh rated at the same point**, so both reported the same contact stress | Diagnosed twice before it was right. The pressure at an instant *is* shared — Hertz reaches the contact through the gap, which depends on the individual radii only as `1/ρ₁ + 1/ρ₂`, and a half-space does not know its own curvature — so the first correction moved the figure to the mesh and gave the gears none. That was the wrong fix for a real complaint: the gears differ not by curvature but by **evaluation point**. Pitting starts in the dedendum, each gear's root is loaded alone at its own end of the path, and each is rated at the worse of that and the pitch point — ISO 6336-2's `Z_B`/`Z_D`, derived rather than quoted. On the reference 17/43 pair: pinion 692.7 MPa, wheel 629.9 |
| [reference.md#trains](reference.md#trains) | An automatic face width sized from **one gear's** requirement | Latent until the two gears' asks could differ, which rating them at their own points made routine — and which two different materials had already made possible. The mesh is carried at `min(b₁, b₂)`, so a gear sized to its own smaller figure pulls the effective width, and the other gear with it, under what that other gear needed: a 250 MPa wheel asked for 29.8 mm and the stage carried 8.5. An automatic width now resolves to the largest ask in its mesh, and a planet answers to both of its. Gated as the invariant the control claims — at an automatic width, every enabled rating is met — which fails on the old code with "gear 0 needs 8.483 mm, mesh carries 7.003" |
| [reference.md#trains](reference.md#trains) | A gear's reported `torque` was the **peak load case**, not the forward torque | `StageTorques::at(Peak)` is `max(|forward|, |backward|)`, which is right for a *rating* and wrong for a label. Where a back-driving load was the larger, the "Torque" row and the "Back-driving torque" row beside it showed the same number — the only condition under which the second row is displayed at all, so it was the only thing it ever showed. The label is the forward torque now, and the peak rating still uses the worse direction |

| [rationale.md#the-language-list-is-rusts-and-so-is-the-tag-matching](rationale.md#the-language-list-is-rusts-and-so-is-the-tag-matching) | The catalogue crossed the boundary whole, but the **language** was hard-coded on both sides | `strings()` took no argument and answered English; the front end had nowhere to ask what else existed. The file layout had said "one file per language" since the catalogue was written, so the gap was in the doors, not the design — which is why adding three languages moved no message and changed no key. `languages()` and `resolve_language()` cross now, and a browser's `zh-TW` is resolved beside the list it resolves against rather than in TypeScript |

| [rationale.md#the-language-list-is-rusts-and-so-is-the-tag-matching](rationale.md#the-language-list-is-rusts-and-so-is-the-tag-matching) | The language list held in a **plain module variable**, so the picker drew itself once — empty — and never again | The third time this exact fault has been recorded, and the second in the same module's neighbourhood: the catalogue lives in `strings.svelte.ts` *because* it arrives after the first render, and the list of languages arrives at the same moment from the same call. It went into `core.ts` anyway, where nothing is reactive, so `{#each languages()}` rendered before the core answered and the `<select>` stayed blank. Moved beside the catalogue, in `$state`, where "which words" sits with "the words" |
| [rationale.md#the-helix-factors-are-a-pair-and-this-tool-can-take-neither](rationale.md#the-helix-factors-are-a-pair-and-this-tool-can-take-neither) | **`Y_β` was excluded for the wrong reason, adopted for a worse one, and is excluded again** | The documentation said omitting the helix angle factor over-predicts a helical root stress, `rationale.md` putting a floor at `Y_β = 0.75` and `state.md` the omission at "up to ~25 % conservative". The floor is the **2006** formula; ISO 6336-3:2019 Formula (66) is `(1 − ε_β·β/120°)/cos³β`, which exceeds 1 over most of its own Figure 8. So `Y_β` was applied — and that was the real error, because the 2019 Foreword lists its first two changes as a modification of `Y_β` **and** a modification of `Y_F`, *together*: the revised `Y_F` carries an internal `f_ε ≤ 1` and the revised `Y_β` gained the `1/cos³β`. Multiplied out, `f_ε · Y_β` is **0.73–0.79 at full axial overlap** — the standard *lowers* a helical root stress there. Applying `Y_β` alone moved this tool from 1.26–1.36× a published ISO rating to 1.29–1.46×: further from the standard in the name of getting closer. Reverted. Two further mixings had gone unnoticed with it — this crate's `Y_F` is measured at a parabola section where `Y_β` is fitted to a 30°-tangent closed form, and its virtual gear uses the 2006 `z_n = z/cos³β` against 2019's `z/(cos²β_b cos β)`. **The old text's conclusion was right and its stated mechanism was wrong**, which is the worst way to be right: nothing in the reasoning could catch the reasoning. `tools/iso_6336_3_stack.py` multiplies the set out now, because the argument for declining a factor is that it is balanced only as a set, and that argument is worthless unasserted |
| — | ...and the lesson, which is general | **A factor's direction is not a property of the factor; it is a property of the set it was calibrated in.** `Y_β > 1` is a true statement about `Y_β` and says nothing about whether omitting it is safe. The same trap is live for anything else quoted out of a balanced standard, and the only defence is to multiply out what the standard applies and compare against what this tool reports |
| — | ...and one thing the round trip left behind that was worth keeping | The measurement found the regime the old documentation never named: at **low overlap** (`ε_β ≤ 0.3`) with `β ≥ 20°` this tool reads **0.78–0.94×** ISO — below it, the unconservative direction, where at full overlap it is comfortably above. It needed no new note: a helical stage without full axial overlap already raises `stage.overlap_below_one`, arrived at independently because such a gear transfers load like a spur gear. That note is the marker for both now |
| — | ...and the machinery it cost, then gave back | `ε_β` depends on the face width, so applying `Y_β` broke `σ_F ∝ 1/b` — `reference.md`'s "the `b` cancels" — and bought a two-branch closed-form solve, a width law threaded through `Loading`, and a fourth argument on `min_face_width_bending`. All of it correct, all of it gated, and all of it deleted with the factor. **A simplification that arrives by deleting a thing is worth more than one that arrives by writing a thing**, and it is recorded because the machinery was genuinely good work in service of a wrong answer |

| [state.md](state.md) | **The bending model was three sources deep and matched to none of them** | The section came from Savage, Rubadeux & Coe; the notch factor from ISO; and the fillet radius fed to the notch factor from neither. ISO's `ρ_F` is the radius *at the critical section*; Dolan and Broghamer's `ρ_f` is "the minimum radius of the fillets". This tool read it at the fillet **junction** — the flattest point the fillet has, 1.4–4.1× the minimum externally and 2.1–6.3× on a ring — so `q_s`, which is inverse in it, came out that many times too small. On a ring it landed on the floor of `Y_S`'s band with two designs in three clamped there. One model now: the parabola section with `K_f`, each notch fit reading the radius it was fitted to, and `RootSection` carrying both so neither can be fed the other's. `σ_F` 69.2/63.4 → **74.3/63.8** on the canary, conservative; `σ_H`, ρ and η unmoved. On a ring the two coherent sets went from disagreeing by up to 25 % and not agreeing on the sign, to agreeing within about 10 % |
| — | ...and the junction was chosen for a good reason, which is why nobody looked again | It replaced reading the *involute's* own curvature, which jumped 0.61→22.9 mm across one tooth. It does remove that discontinuity. What was never asked is whether it is the right point on the fillet, and by either source's definition it is not — a fix that solves the problem it was aimed at can still be wrong about the one it was not |
| — | ...and the selection rule, found in the same reading | The paper searches both curves and takes "the smaller x coordinate", the weaker parabola. This crate took the fillet whenever it had a tangency and consulted the flank only otherwise. The two agree whenever one curve has none — every ring — so ring results never depended on it; an external tooth's could. Gated by `the_reported_parabola_is_the_weaker_of_the_two`, which rebuilds the condition rather than restating the code |
| — | ...and an invariant that turned out to be an observation | "Load sharing relieves every tooth it reaches" was asserted for years and held until the notch model changed. It is not an invariant: the swept maximum is a product of a form factor rising toward the tip and a share falling away there, so a member whose factor rises steeply enough is governed near its tip at a partial share, above what the unshared convention assumes. A planetary ring now comes out 2.4 % *higher* with sharing on. The test asserts reach, which is what its name always claimed |

| [reference.md#bending](reference.md#bending) | **A ring's critical section was located at 30°**, the external gear's angle | ISO 6336-3:2019, 6.1 specifies 30° for external gears and **60° for internal** ones, 6.2.5 saying the same thing as a sign convention on the shared construction. A ring's fillet curls into the rim where a rack-cut one curls away, and the tooth flares into it faster, so the two angles are not interchangeable — though **not** for the reason the first version of this entry gave: a ring's tooth widens from tip to root exactly as an external one does (0.967→2.569 mm at z=40, `gear-cli matrix` study 6), and the claim that it was "thick at its tip" was measured out of the documents rather than into them. It affected `CriticalSection::TangentAngle` only — the crate's default is the inscribed parabola, which finds its own tangency from the load point and needs no such constant — so nothing shipped moved, and the correction is that the retained standards-comparable setting was not comparable on a ring. The angle is a number on `ToothOutline` now, beside the curves and the frame flip — the **one** quantity in that construction whose value depends on the kind of member, the search reading a target rather than testing what it is running on. The parabola never asks for it: its tangency condition carries no angle, and being odd in `y` it is the same equation either way round |
| [reference.md#bending](reference.md#bending) | The `Y_S` notch band was **a citation of a citation**, and it was right | `1 ≤ q_s < 8`, carried for a year as second-hand because the standard was paywalled and unread, with `strength.rs` recording it as such and `state.md` listing "read ISO 6336-3" as work outstanding. Read: ISO 6336-3:2019, 7.2 states Formula (62) "valid in the range: `1 ≤ q_s < 8`". Nothing moves. Recorded because a check that confirms a number is worth as much as one that changes it, and because the same clause carried something the citation had dropped — 7.1 says the fit is derived from **external spur gears at `α_n = 20°`** and gives only "approximate values for internal gears and for gears having other pressure angles", both of which this crate does |

| [reference.md#the-boundary](reference.md#the-boundary) | The shell let overflow reach the **document**, giving the page a third scrollbar over nothing | Two panes that each scroll, inside a `height: 100vh` grid — but a row that outgrows a fixed-height container overflows it, and an overflow the container does not handle is passed to the viewport. The shell is an explicit frame now (`overflow: hidden`), which makes the class impossible rather than unlikely, and `100dvh` before `100vh` because `vh` ignores a horizontal scrollbar and so overshoots by exactly one scrollbar's height when a page grows one. **Not reproduced headlessly** — headless Chromium reports `innerHeight == clientHeight`, so it has no classic scrollbars to overshoot against; what *is* verified is that the frame clips nothing, with both panes still reaching their last child |

| [reference.md#languages](reference.md#languages) | **Forty-odd sentences never became keys at all**, so four languages shipped with English scattered through them | `tools/check_strings.py` holds the catalogue and the sources to each other in both directions — and cannot see a sentence that was never a key, which is the one thing it was there to catch. What was left behind was mostly the *words around* numbers: a stage heading, "driven forward", the "at" before a gear's name, "to" between the ends of a range, a fresh tab's name, the units `rpm` and `MPa` in the places they were typed rather than read from their own keys. Found by sweeping the front end for English rather than by trusting the tool, and the sweep is now what the reference tells the next person to run |

| [reference.md#the-boundary](reference.md#the-boundary) | The gap *inside* a group of fields taken for the gap *between* groups | The gear tab's Type field sits in its own group — it decides which fields follow it — and a ring's cutter in a third, so both sat flush against the group below while every other pair of fields was one field-gap apart. A flex `gap` says nothing about siblings of the container, and the fault is invisible until a group has exactly one member: measured at 0 px against 12 px everywhere else |

| [rationale.md#notes-must-not-move-the-controls](rationale.md#notes-must-not-move-the-controls) | A `select` left **transparent**, so the browser painted it and the page painted the text on top | The two do not agree in a dark theme: the text came from a token and the box from the platform's native control, which stayed light. Only the gear tab's selects were right, and only because they happened to name a background — the geartrain's and the language picker's said `background: none`, which is not "no background" but "whatever is underneath", and underneath a form control is the widget. Measured at 16.56:1 light and 14.63:1 dark once every select named its own. The same pass narrowed `color-scheme` per theme: it was declared once as `light dark` and never stamped, so under an explicit `data-theme` the tokens would answer to the attribute while every native control answered to the operating system |

| [reference.md#contact-stress](reference.md#contact-stress) | **A crossed pair's contact rated on the elliptical solution alone**, with no floor from the contact line its teeth actually have | The elliptical solution assumes half-spaces of *unlimited extent*: as the shafts come parallel its patch lengthens without bound and the peak pressure it reports falls toward **zero**, while the real pair carries the same load on a line the face has not lengthened at all. At `Σ = 0.5°` the stage reported **73.1 MPa where its own teeth carry 576.5** — an eightfold under-prediction, in the unconservative direction, on the geometry nearest the parallel limit. The parallel path had carried `max(σ_elliptical, σ_line)` since general contact arrived and could not see the omission, because for it the elliptical term is *exactly* zero and the `max` is the line term unchanged — so the two paths agreed everywhere the parallel one was asked and nowhere it was not. One `hertz::peak_pressure` now, asked by both. **A worm is untouched**: at 90° the patch is a fraction of the face, the ellipse governs, and both canaries are bit-identical — which is the same evidence read the other way |
| [reference.md#crossed-axes](reference.md#crossed-axes) | `CrossedPath`'s own documentation still said **four tangent lines, of which one passes through the pitch point** | Eight and two, corrected in the log above and in `mesh_branch`'s doc — but the *module's* description of the same construction was never updated with it. Nothing computed from it, which is exactly why it survived: a construction described in two places has a copy that nothing exercises, and prose is the copy no test reads. The same pass found `involute.rs` promising "three of the **five** transcendental solves" where the rationale has counted nine since the crossed work landed |
| [rationale.md#no-engineering-calculation-in-typescript](rationale.md#no-engineering-calculation-in-typescript) | The gear tab seeded an eccentric **centre-distance throw** from a `0.1` written in TypeScript | The same class as the cutter default that started this rule, found by sweeping the front end for engineering numbers rather than by anything failing: every other default a fresh tab starts at crosses from `gear_wasm::defaults`, and this one did not, so it could drift from the geometry it seeds without either side noticing. It crosses now |
| [reference.md#rack-and-reference-geometry](reference.md#rack-and-reference-geometry) | `tan α_t = tan α_n / cos β` and `sin β_b = sin β · cos α_n` written out **nineteen times** between them | Ten and nine, five of the latter in `screw.rs` alone, every one of them correct — which is what makes it the pattern rather than a bug: *one idea written down twice is a place where two answers can differ, and the copy nothing exercises is the one that is wrong.* They are `crate::plane`'s two functions now, and the refactor was gated on the digits not moving: both canaries bit-identical, 437 tests unchanged |

| [rationale.md#load-sharing-is-offered-rather-than-assumed](rationale.md#load-sharing-is-offered-rather-than-assumed) | Load sharing reachable **only from a CLI diagnostic**, so the one estimate the crate ships was hidden rather than offered | `LoadSharing::LinearRamp` existed, was documented as a placeholder, and no user could reach it — which sits badly with a project whose rule is that estimates are *exposed* features rather than hidden ones. Made a stage input, off by default, reaching bending alone. Wiring it surfaced the thing worth knowing: at a **virtual contact ratio of 2 or more** — an ordinary high-contact-ratio tooth, addendum 1.35 — there is no single-pair zone, the ramp never reaches a full share, and it relieves the tooth by about **a third**. A large number from an uncalibrated model in the unconservative direction, so the stage says so where the figure is shown. A standard tooth cannot reach that regime at any helix angle, which is why the case that fires the note carries an addendum rather than an angle |

| [rationale.md#the-language-list-is-rusts-and-so-is-the-tag-matching](rationale.md#the-language-list-is-rusts-and-so-is-the-tag-matching) | **A tab kept the name the application gave it in a language the reader has since left**, so an English session showed Chinese tabs | The name a fresh tab starts with is the catalogue's word, read once at creation — on the reasoning that it becomes the reader's document name the moment they type over it. True of a name they *have* typed over, and false until then: until then it is still the application's own word and has to follow the language like every other label. Read as a bug in the picker, since what a reader sees is the words half-changing. Switching now carries the untouched defaults across, and one a reader has named cannot match the outgoing default so it is left alone. **Diagnosed by elimination rather than on screen** — the catalogues were checked key for key and swept for stray script, the resolution and the swap were read, and the front end was grepped for labels captured outside the reactive read — because there was no browser in the environment to run it in, which is the first time that has been true and is worth knowing about the diagnosis |
| [state.md](state.md) | ...and the second half of the same report: **two copies open in one browser disagreeing about the language** | Not a leak — the audit found exactly one thing that crosses between copies on an origin, the stored preference, and no cookie, service worker, IndexedDB, shared worker or cache besides. A preference *should* be shared; what it should not do is change under a copy that then never hears about it, which leaves the two disagreeing until one is reloaded and reads exactly like the earlier fault. A `storage` listener follows it now. The value is a resolved language code and anything unrecognised resolves to English, so an older copy cannot be broken by a newer one writing a code it has never heard of |

| [reference.md#internal-gears](reference.md#internal-gears) | **A ring's thickness modification was taken into the cutter's *plunge* rather than its *tooth***, so `k` moved the root diameter | The rule the crate is built on is that *radial* quantities take plain `x` and *thickness* quantities take `x + x_s`, and a ring's root radius is radial — it is where the cutter's tip reaches. An external gear gets this from its rack, whose tooth is narrowed or widened while its depth stays put; a shaper is the same statement on a pinion, and the **cutter's tooth** is what should carry `k`. Taken into the centre distance instead, `k` plunged the tool: on a 43-tooth ring the root swept 22.373 → 23.309 mm across `k = 0.85 … 1.3`, so the ring *scaled* where its teeth should have thinned — a module change wearing a thickness control's name, which is how it was reported. The cutter's tooth is the standard one scaled by `k` now, which is the basic rack's own definition read round a circle, and the plunge answers to `x` alone |
| [reference.md#internal-gears](reference.md#internal-gears) | ...and where it could not be plunged that far, the tool was **silently placed at reference centres** | The same fault's second face, and the worse one. Folding `k` into the shift sum drove `inv α_w` negative for ordinary inputs — `k = 0.7` on a 43-tooth ring, `k = 0.8` with `x = −0.4` — where `operating_geometry` returns `None` and the fallback cut at reference centres without saying so. The profile and the tool then described **different rings**: 0.297 mm apart, against a gate that trips at 5 µm. The comment above the fallback said it was reachable only where `ShaperCut::new` "refuses anyway", and it did not refuse. Nothing but the radial shift can reach it now |
| [reference.md#internal-gears](reference.md#internal-gears) | ...and the reason both survived: **every gate on the cut swept the shift and left `k` at its default** | `a_shifted_ring_is_the_shape_its_cutter_leaves` swept seven shifts and two tooth counts, and one thickness modification: the default. *An axis nobody turns is an axis nobody tests* — recorded in this very log, and met again in the module whose own tests quote it. `k` is swept there now, and a second gate states the rule directly: a thickness modification moves no radius, asserted **exactly**, since the plunge does not depend on `k` and neither can anything the plunge sets. Both fail against the old code, at 0.297 mm and at the root radius respectively |

| [reference.md#internal-gears](reference.md#internal-gears) | **A ring's flank ran to the root circle whether or not the space had already closed**, so the profile crossed its own centreline | A ring's space narrows *outward*, so the two flanks bounding it converge and meet at `inv α = π/z − ψ_b`. Past that radius there is no space left to cut — but the root was taken as where the cutter's tip reached, full stop, and the flank followed it there. Mirrored into a full tooth, the overshoot comes back through its own reflection: an inverted spur at the bottom of every space, in an outline bound for a DXF. At `k = 0.6` on a 43-tooth ring, 14 of 398 profile points sat past the centreline; at `k = 0.4`, 45. The guard is the one already on the *other* end of the same tooth — `inv α = −ψ_b`, where a ring's tooth runs out of thickness inward — and the one `Tooth` puts on an external gear's pointed tip, which is why this is a missing case rather than a new kind of check |
| [reference.md#internal-gears](reference.md#internal-gears) | ...and the path it hid behind | It bit only where **no fillet was cut**, since a fillet truncates the flank first. That happens when the cutter's own tooth comes to a point — which a thick ring tooth demands, because thickening the workpiece thins the tool. So the case needed a low thickness modification *and* the tool refusal that comes with it, and the refusal was already reported (`clamp.cutter_no_tip_corner`) which made the part look accounted for. What went unreported was that the profile it came back with was not a simple closed curve |

| [rationale.md#a-rating-that-cannot-be-taken-costs-the-rating-not-the-stage](rationale.md#a-rating-that-cannot-be-taken-costs-the-rating-not-the-stage) | **A ring that could not be rated for bending failed the whole planetary set** | No notch means no `Y_S` means no bending number — but the ring's geometry draws, exports and meshes, and the ratio, both contact stresses, the efficiencies, the cycles and the other two members' bending never needed the notch. The set refused all of it. The reachable case is ordinary rather than exotic: a set gives its ring `k = 2 − k_stage`, so a stage modification of 1.4 puts the ring at 0.6 and the cutter that would leave its space comes to a point before its own tip. Every figure but the one is reported now, with the member's own clamps beside the blank saying why. *The tool is for informing a decision, not for taking it.* |
| — | ...and on the way, an error that named the wrong cause | One variant served both "this tooth is too undercut to have a root section" and "this ring's cut left no fillet", so a ring with no tool read as an undercut tooth — nothing is undercut, and what relieves it is a bigger cutter rather than a different shift. Briefly given its own variant; then the case stopped being an error at all, and the variant went with it. **The second change deleted the first**, which is the better outcome of the two and worth recording as such: a diagnosis nobody has to read is better than a correct one they do |

| [rationale.md#a-reversed-root-is-disclosed-and-corrected-only-on-request](rationale.md#a-reversed-root-is-disclosed-and-corrected-only-on-request) | **The reversed-bending derate applied to planets only, and silently** | 0.7 on the cyclic bending allowable, applied to a planet whether or not anyone asked — while the **reversing drive** flag, an explicit input on a control that says it reverses the load, derated nothing anywhere. One convention, applied where nobody chose it and absent where they did. It is a train-wide switch now, off by default, reaching every root that is loaded both ways; and where it is off the stage says which members those are, rather than a reader inferring it from a stress that did not move. Found from the other end: a planet's bending barely responding to its own tooth thickness, because a figure nobody had been told about was already in it |
| — | ...and the derate reached the **contact** rating too | `asks_of` took one allowable and used it for both minimum widths, so the planet's contact width was sized against 0.7 × fatigue as well. Reversal is a bending allowance; pitting is compressive on whichever flank carries it. Two allowables now, and only the bending one can be reduced |
| [reference.md#bending](reference.md#bending) | `notch_parameter_in_range` **existed and nothing asked it** | `docs/rationale.md` has said all along that the `Y_S` fit's notch band is "clamped into the fit's stated range and **reported raw**", and `strength.rs` that a result outside it "says so instead of quietly returning a boundary value". Nothing said so: no stage and no boundary type ever called the function. Outside the band the correction is taken at the boundary, and above it that **under-predicts** root stress — the unconservative direction. Both stages report it now, naming the member and the value. It fires on ordinary inputs: a planet at `k = 1.3` sits at `q_s = 0.97`, just under the band |

| [reference.md#the-boundary](reference.md#the-boundary) | **A note naming a member, in a list keyed by note key** — so a stage that raised it for two members could not be drawn at all | The front end draws every note list as a Svelte keyed `{#each … (n.key)}`, and duplicate keys in one are an error rather than a warning: the block throws and the stage stops rendering. Switching a train to reversing raised `stage.reversed_bending_uncorrected` for the sun, the planet **and** the ring — one key, three entries — so the planetary stage would not expand. Reported as a hang, and it was a thrown render. Two fixes, and both were wanted anyway: a note about a gear lives **on the gear** now, one per list; and every note list is keyed by position, so a repeat can never again be the thing that stops a panel drawing. The same shape had been latent in `stage.face_width_no_source`, which names a member and can fire for both |

| [reference.md#export-and-import](reference.md#export-and-import) | **Every DXF this project ever exported was structurally incomplete** | AC1015 is a graph: six sections, nine symbol tables, both spaces defined as blocks, a root dictionary, and an owner handle on every record. The writer emitted three sections and two tables, and no entity said which layout owned it. Nothing caught it — the Rust tests checked the writer against our own reading of the format, and `ezdxf`, the independent reader, invented the missing structures on load and reported no fault. It surfaced the only way left: **SOLIDWORKS would not open the file.** Gated now against the published minimum, tag by tag, and against a handle graph that must close — both verified by breaking the writer and watching them fail, and the corrected file confirmed importing |

| [reference.md#efficiency-parallel-axes](reference.md#efficiency-parallel-axes) | **The shift optimiser rated a pair the stage would not build** | Turned on, it drove the shift sum to 3.8 — because it built its candidates with the *manual* addendum and rated them at the *zero-backlash* distance, when the stage resolves an automatic addendum for tip width and runs the pair a clearance further out. It was optimising a pair nobody assembles, and the answer it liked best was the one furthest from the one that gets built. The search takes the stage's own `params_at` now, so what is optimised is what is reported. The same fix, in the same words, as rating contact at `mesh` rather than at `operating` — the second time this exact confusion has cost an answer |
| [reference.md#efficiency-parallel-axes](reference.md#efficiency-parallel-axes) | **Each search asked its own questions of a shift**, so a bound reached the one it was written in and no other | Four things stop a tooth existing: below the undercut floor, undercut anyway, pointed before its tip, or a root round that no longer fits. The pair asked all four. The epicyclic set asked one, of one member. The hula stage asked two, of one member — and was choosing a pinion whose root round could not be cut, taking 1.9 points of efficiency less for it than when told not to. Written once as `auto::member_is_buildable`, and the test asks the invariant rather than the wiring: whatever a stage chooses must be cuttable |
| [rationale.md#notes-must-not-move-the-controls](rationale.md#notes-must-not-move-the-controls) | **A field's note used to draw a readout**, where it right-aligned the annotation away from its value and reserved a line that could never fill | The slot exists so typing does not move the page. A figure that cannot change while you look at it needs none of it. The hula stage's readouts had been built that way throughout, and its clamps were a run of the note that is pulled *up* against the field above it — stacked, each closed on the one before instead of reading as a list |
| — | **A row's alignment lost to a selector two classes stronger, silently** | `.switchrow` set one column and `justify-items: end`; `.grid.shared > label` sets three and carries two classes and an element to that one class, so the three-column template stayed underneath and the switch was placed at the end of the *label* column, mid-row. The gear panel's container has no such rule, which is the only reason identical markup landed correctly there and the fault read as "the gear panel broke the train panel". Racing specificity needs a selector naming every container the row might sit in and goes stale at the fourth; the row is a flex column now, which a column template cannot reach at all. Then `align-items: center`, harmless on the grid it was written for, meant "centre every child horizontally" on that flex column — the override that had been hiding it was the very rule that moved |

| [rationale.md#an-input-does-not-wait-on-an-answer](rationale.md#an-input-does-not-wait-on-an-answer) | **The failure was the one thing the application said in a language nobody chose** | `TrainError` has carried a `note()` since it was written, and its keys are checked in both directions by the string tests — but the WebAssembly boundary mapped the error through `Display`, which is English prose written in Rust. Every other message crosses as `{ key, values }`. It also threw, so a geartrain that would not build — an ordinary thing to be holding mid-edit — arrived as an exception rather than as data, and the panel had nothing to show but a raw sentence. It crosses as a note and a stage number now, and only a boundary that actually broke is still an error |
| — | **Naming a grid column is not naming a cell** | The box was written before the switches so that the row's `<label>` would target the box — `<button>` is labelable, and with the switches first, clicking a field's *name* pressed its `auto` toggle. Reading order was then restored with `grid-column`, and auto-placement does not go backwards: an item placed in column 3 leaves the cursor past column 2, so the switch that belongs there starts a new row. Every toggle and unit dropped a line below the box it qualifies. Each of them names its row as well now |

| [reference.md#tooth-cycles](reference.md#tooth-cycles) | **An epicyclic set counted the wrong revolutions**, three ways, and each was a different member | A tooth is engaged once per turn *relative to the carrier*, per planet — and the sun counted its own revolutions instead, 1.4× too many on the shipped set. The ring counted the sun's, which is 3.5× too many and, worse, is a figure with no reading at all: a held ring does not turn, so a count taken from its own revolutions would be **zero** on teeth that are loaded every time a planet passes. Only the planet was carrier-relative, and it was referred to the *sun's* speed rather than to the input shaft's, so it was right only in the arrangements where those are the same shaft. Found by asking what the hula stage's answer should be — its crank *is* the carrier, so the rule had to be stated to fill in a field the stage did not have — and then asking why the set beside it said something else. One rule now, `train::engagements`, and the test checks each member against the speeds the result reports rather than against the rule that filled it |
| [reference.md#planetary-sets](reference.md#planetary-sets) | **A planet's speed was the carrier's**, and its speed relative to the carrier was the sun's | `speed_absolute` read the shaft array by role, and a planet is not one of the three shafts — the entry under its role is the arm it rides. On a set with the ring held the two are not even the same sign. `speed_relative`, the figure the panel labels "what its teeth actually see", was `ω_s − ω_c`: the sun's relative speed, which differs from the planet's by `z_s/z_p` — a third, on the shipped counts. Both survived because the planet's own kinematics were never written down: `planetary::power` is about three shafts and a basic ratio, and a basic ratio does not say what runs between the two central members. `Power::planet_speed` derives it where the rest of the kinematics live, gated against the *ring* mesh, which reaches the same number through tooth counts the sun mesh does not use |
| [reference.md#load-cases](reference.md#load-cases) | **A member was rated at its own face width rather than at its mesh's** | The stress was taken at the width the pair shares — correctly, since that is the width the load is spread over — and then handed to `min_face_width_*` as though it had been measured at the member's own. Where a member is wider than its mate the two differ, and the minimum comes back over-stated in exactly that proportion. The spur stage had it right and the planetary set did not, which is the tell: *one idea written down twice*, and the copy nothing exercised was the wrong one. `MemberRating` is the one place now, and both epicyclic kinds ask it |
| [rationale.md#inputs-are-the-only-state](rationale.md#inputs-are-the-only-state) | **A planet reported its shift and its speed twice each**, and the panel read the copies | `PlanetResult` carried `profile_shift` and `speed_absolute` beside the `GearResult` that already had both. Harmless while they agreed — they were the same expression evaluated twice — and the point is that they agreed all the way to being *wrong together*: the fault above was in both copies, and the copy is what the front end had reached for. Gone; a member's shift and speed are its `GearResult`'s, and what stays on `PlanetResult` is what no `GearResult` has a field for |
| [reference.md#the-hula-stage](reference.md#the-hula-stage) | **A hula pair with no path of contact came back with a row of zeroes** | A contact ratio of zero, an efficiency of zero, and that zero fed to the power flow as the pair's fixed-carrier efficiency — from which the stage reported an efficiency, a set of shaft torques, a backlash and three speeds, none of which mean anything and none of which said so. It is refused now, as it is in every other stage kind. The find is worth the entry for how it surfaced: a test asserting that a hula's two modules want to be equal had been comparing one stage that transmits against four that do not, and it only failed once the refusal existed |
| [reference.md#the-hula-stage](reference.md#the-hula-stage) | **A documented table did not match the command it names**, and neither did the sentence above it | `gear-cli hulaband`'s figures are quoted in the reference, and every row had moved: 31.3 → 32.3 % at one tooth of difference, 48.0 → 45.1° beside it, and the stage the tool *ships* with described as keeping "73 % forward and 63 % back" where its own addendum table, five lines below and generated from the same default, says 79.6 %. Found by running the command while auditing something else, and confirmed stale rather than newly broken by building the previous commit in a worktree and getting the identical output — which is the check worth recording: *a figure that changed is not evidence that today's change moved it.* The shipped stage's reduction and both efficiencies are a canary now, so the next paragraph that goes stale fails a test first. **The generated tables around it were re-checked and hold**: the addendum sweep is exact to every digit it prints, and the two contact-ratio derivations a hula mesh has agree to 3 × 10⁻¹⁵ over 208 pairs |
| [reference.md#the-hula-stage](reference.md#the-hula-stage) | ...and two more of the same section's tables, found by regenerating all of them | One row of the reduction table (98.74 → 98.80 %, 35.8 → 36.8 %) and **the whole module-ratio table**, which had been generated from a fixture the tests no longer use — the one whose short teeth at a negative shift do not transmit away from equality, replaced when the refusal for a pathless pair arrived. The mechanism the table illustrates was never in doubt and still reads: the offset holds below equality and rises above it. Every figure the section quotes was re-derived, and the ones that hold are worth naming too — the 57° at half a millimetre of gap, the four-row arrangement table, the addendum sweep. **The tables are a canary now**, asserted to half of the last digit the document prints, so the next one fails a test rather than a reader |
| [reference.md#who-decides-a-shift-and-what-it-must-satisfy](reference.md#who-decides-a-shift-and-what-it-must-satisfy) | **A gear in a geartrain never said it was undercut** | A gear *tab* has shown it since undercut existed; a member of any stage kind showed nothing, and `Tooth` records only severing as a clamp because only severing alters the profile. It matters exactly where the `no undercut` control cannot help: that control bounds a shift somebody chooses, and a shift a **relation** leaves over is chosen by nobody — pin a hula mesh's ring and its pinion takes the whole of a negative `Σx`, past its own floor, with the toggle on and nothing able to move it. Reported now on every rack-cut member of every stage kind, as a remark rather than a clamp — nothing was altered — and the message is the one the gear tab already uses rather than a sixth translation of the same sentence. Found by asking what the hula stage does with a bound it cannot honour, and finding that no stage does anything |
| [reference.md#internal-gears](reference.md#internal-gears) | **A ring was asked about its tip width**, and answered about a tooth nobody is cutting | `no undercut` is withheld from a ring throughout — its flank is its shaper's, so a rack's question has nothing to bound. The bound on the *other* end of the tooth is the same statement and was asked anyway: `addendum_asked` builds a `Tooth` from the member's parameters, which for a ring is the external gear those numbers would describe rather than the ring itself, and reports its tip width as this member's. It fired on **both rings of the shipped hula stage** at a 2 mm minimum. The planetary set had never asked it, which is the tell — two stages, one question, two answers. Withheld in the core and withheld in the panel, where the control and its minimum tip width had been offered on a ring card and reached nothing at all |
| [state.md](state.md) | **An epicyclic stage's third shaft reported its speed and torque to nobody** | A set has three shafts and two of them carry a member whose card prints both figures. The carrier does not, and it is regularly the input or the output — so `PlanetaryResult::speeds` and `::torques` were computed, crossed the boundary, and reached no screen; the hula stage's crank was the same. Found by sweeping the *outputs* rather than the code: every field of every result type, against everything that reads one. That sweep is the one this log already recommends, and it is the first time it has been run against the result types rather than against a construction |
| [reference.md#load-cases](reference.md#load-cases) | **"Said rather than divided by", and then divided by** | An automatic face width with all four rating sources switched off resolved to **zero**, which the note beside it described and the stage then used as a width: every stress came back `inf` and every minimum width `NaN`, on all three kinds that have the control. `serde_json` writes both as `null`, and the panel blanks a `null` — so the browser was honest *by accident*, while the CLI printed `inf` and the generated TypeScript said `number` of a field that could arrive `null`. An automatic value with nothing to choose between has nothing to choose, so it stands at the number already in its box. Found by asking what each result field can be rather than what it usually is |
| [reference.md#load-cases](reference.md#load-cases) | **A planet's root was rated against one of its two meshes** | The sun loads one flank and the ring the other, and only the sun's figure was taken — while the *contact* rating had taken the worse of the two all along, written out as a `max` of two named quantities. The two meshes carry the same tangential force, so the gap is invisible on an ordinary set: what separates them is the section each mesh's contact ratio puts the load at and the width that mesh carries it over. Give the ring a **3 mm face against 10 mm elsewhere** and the ring mesh loads the planet's root to **45.6 MPa where the reported figure was 16.6** — a 2.7× under-prediction in the unconservative direction. The first draft of the test did not catch it: both its fixtures were sun-governed, so it passed against the very code it was written to condemn. *A test for "the worse of two" needs a case each way.* |
| [rationale.md#a-control-that-exposes-an-assumption-must-not-default-to-it](rationale.md#a-control-that-exposes-an-assumption-must-not-default-to-it) | **The correction reached one of the two surfaces that had the fault** | `working_depth` — the depth the undercut question is asked at — was a fixed one module, and was changed to follow the gear's own dedendum precisely so that the reported threshold and the profile generator's own `undercut` flag would "agree by construction rather than by coincidence". A **stage** does that. The **gear tab** kept the one module, on the stated argument that the tab has no control for it — but having no control is a reason to *derive* a value, not to pick a convention. On the default 17-tooth gear the two differ by a quarter of a module and differ in **sign**: undercut below `x = −0.2443` on the tab against `+0.0057` as a stage member, and the generator settles it — at `x = −0.1` it reports the flank undercut while the tab's own threshold said it was not. So one gear had two bounds depending on which panel you opened, and the tab's contradicted the flag printed beside it. **534 tests passed against it**, because each surface was only ever asked about itself. The gate is an equality between the two paths rather than an assertion about either number — the number is a consequence, the agreement is the claim |
| [reference.md#load-cases](reference.md#load-cases) | **The load-sharing ramp's two halves did not meet**, and the sweep that reads it could not converge | Above a virtual contact ratio of 2 there is no single-pair zone and the entering and leaving ramps overlap. Split at `d = ε − 1`, the entering ramp reached `RAMP_MAX` there while the leaving ramp had come only `(ε−1)⁻¹` of the way — a **step of 0.030 in the share at ε = 2.1**, growing with the ratio, at a point where nothing physical happens. It did not surface as a wrong number. It surfaced as a **convergence failure**: the bending sweep's maximum sat on the high side of the jump, so quadrupling the sweep kept finding a slightly larger answer (0.34 % at z=12, ε=2.1) and 200 samples were declared converged by nobody. *A discontinuity in a model reads as a resolution problem in whatever samples it, and the instinct is to raise the sample count.* Written as `min(d, ε − d)` — the nearer end is the one a point ramps from — which is **bit-identical below ε = 2**, since outside the plateau either `d < ε − 1 ≤ 1 < ε − d` or `d > 1 ≥ ε − 1 > ε − d` and the `min` picks the term the branch picked. Two branches became one expression and a step became nothing, which is the usual shape here |
| [reference.md#load-cases](reference.md#load-cases) | **A self-locking worm's wheel reported 2.2e307 N·m of back-driving torque** | The load enters at the output and `back_driving_torques` refers it to each stage's input shaft, applying that stage's backward efficiency only when passing it *up* to the next one. The worm stage divided by the same efficiency again bringing it back to its wheel, on the reading that the mesh loses something carrying it — which it does, once. A worm's backward efficiency is **zero** whenever it self-locks, and self-locking is the reason a worm is in a lifting drive at all, so the case the feature exists for is the case that blew up. It was **finite**, so it crossed the boundary as a number rather than as the `null` an infinity becomes and would have drawn on screen as a figure — the null-walk gate could not see it. Nothing caught it because **every train the harness ships sets `back_driving_torque` to zero**: the golden corpus records what the tool prints and the tool was never asked. Found by reading the four places that computed this field, which is the sweep this log recommends and which had never been run on this one |
| [rationale.md#each-stage-kind-keeps-its-own-result-type](rationale.md#each-stage-kind-keeps-its-own-result-type) | **A crossed pair's members said nothing about their own teeth** | "A gear in a geartrain never said it was undercut" is already an entry in this log, and its fix says *every rack-cut member of every stage kind*. It reached four of five. A crossed pair is a spur stage with an axis angle, solved by translating it into the equivalent screw pair — and that translation keeps the tooth counts, the materials and the face widths and **drops the tooth form**, because the thing it is translating into is a worm, and a worm is a thread. So the shift, the dedendum and the root round the designer typed reached nothing, and neither did any clamp raised on them: the same 9-tooth pinion reported `clamp.tooth_undercut` with its shafts parallel and **nothing at all** with them crossed. Built from the `StageGear`s on the crossed side, which is the only side that has them. The first draft of the gate was wrong in an instructive way — it asserted the crossed answer *equalled* the parallel one, and a crossed pair's teeth are genuinely different teeth, since the shaft angle **is** the helix angle doubled and 45° of helix lifts `α_t` to 27° and removes the undercut outright |
| [reference.md#the-hula-stage](reference.md#the-hula-stage) | **A crank offset given by hand was not the offset the stage ran at** | `HulaStage::offset` says of itself that it has "the same shape every stage's centre distance has, because it is the same decision … a number given by hand is the distance to run at". A spur stage means that: a given centre distance is the running one and the zero-backlash geometry is derived by taking the absorbed clearance back out of it, `manual − clearance_taken()`. This one handed the given number straight to the solve, which made it the **nominal** offset, and then added the running clearance on top. So the tool reported 0.8268 mm, and a designer who typed that back got 0.8468 — and typing *that* back would have widened it again. **Not idempotent under its own output**, which is the property a user meets rather than the arithmetic. Found by asking where each stage kind's clearance rule lives and finding three methods named `clearance_taken`, two answering one question and the third a different one — the hula's covers its far-side gap, and its *running* clearance had no such rule at all |
| [reference.md#the-centre-distance-a-pair-runs-at](reference.md#the-centre-distance-a-pair-runs-at) | **The reported clearance was the input read back, not the gap the stage runs at** | A centre distance is the true distance and a clearance is what portion of it is clearance, so the two distances a result already carries and the clearance between them are two facts and a subtraction. It was a fourth statement of the same quantity instead: the input echoed where something was free to absorb it — right when the distance is automatic, because the running distance was *built* by adding it — and **zero** otherwise. Given a distance, it was wrong every way it could be: a pair told to run at 30.3 mm whose shifts put it at 30.0057 reported 0.02 with the shift optimiser on and 0.000 with it off, against an actual gap of **0.294 mm**. The test that covered it asserted the convention rather than the quantity — "the input is not read, and the answer reports that" — which is a true statement about an *input* standing where the answer about a *gap* belongs |
| [rationale.md#direction-is-the-readers-not-the-mechanisms](rationale.md#direction-is-the-readers-not-the-mechanisms) | **A set reported its back-driving torques from the forward distribution** | An epicyclic set solves its power flow twice, once each way, because which shaft drives decides where `η₀` multiplies. It used the second solve for its efficiency and **discarded its torques**, reporting each member's share of a back-driving load as the *forward* torque scaled by the ratio of the two stage torques. That scaling is exact wherever the forward torque is a geometric projection or the two directional efficiencies agree — every parallel-axis pair — and false of a set: the shipped one's ring came out **6 % low**, while the sun was right by construction, being the shaft the normalisation pins. Found by asking, of the rule that a mechanism has no forward, which reverse cases are *written* rather than asserted. The gate is the degenerate case rather than the figure: at zero friction the two directions distribute torque identically, because there is no loss for a direction to place |
| [rationale.md#direction-is-the-readers-not-the-mechanisms](rationale.md#direction-is-the-readers-not-the-mechanisms) | ...and the hula stage had it too | The same fault as the set above, found by finishing the sweep rather than by a second symptom: a hula stage **is** an epicyclic power flow, solves its reverse for the efficiency, and discarded the torques that solve produced. Both read the reverse now, normalised so the shaft the load was referred to carries what `back_driving_torques` referred there. *A rule worth stating is worth sweeping to the end of* — the set was found on the first site read and the stage on the third |
| [reference.md#load-cases](reference.md#load-cases) | **Every train the harness ships set `back_driving_torque` to zero** | So `tools/check_golden.sh` recorded a path nothing ever walked, and the two faults above were invisible to it: a self-locking worm's wheel reporting 2.2e307 N·m and an epicyclic ring 6 % low would both have shown as a diff on the first run. `gear-cli train mixed` carries a reacted load now — mixed being the one with the worm stage, so the load is *reacted* rather than passed through, which is the case the feature exists for and the one every fault in it has been in. **A corpus records what the tool prints, so a path the tool is never asked to walk is a path it cannot cover** |
| [rationale.md#direction-is-the-readers-not-the-mechanisms](rationale.md#direction-is-the-readers-not-the-mechanisms) | **A load case was a magnitude, where it is a torque and a direction** | The two directional torques were collapsed to `max(|forward|, |backward|)` **at the stage's input shaft**, and that one magnitude was then pushed through the *forward* construction. Taking the maximum before the distribution rather than after it is the same answer only where the distribution does not depend on direction — a parallel-axis mesh carries one tangential force whichever way it turns, so the spur stage is unaffected to the bit. It is not the same answer for the two kinds whose distribution *is* directional, and both of those had just had their reported member torques put right for exactly that reason: the reports were fixed and the ratings were left behind, in the same function, with nothing comparing them. A back-driven planetary rated its ring **6.0 % low in bending and 3.0 % low in contact**; the hula stage, whose reduction is far larger, **41 % and 23 % low**. The scale is per *mesh* now, since which direction loads a mesh hardest is a fact about that mesh and a set's two meshes need not agree about it. The gate is a ratio the outputs already carry — every rating is linear or square-root in the member's own torque, so with the duty torque at the peak the two load cases stand in exactly the ratio of the two torques the member reports — and it is run with the load both dominating and not, because a `max` checked on one side of itself is not checked |
| [reference.md#crossed-axes](reference.md#crossed-axes) | **A worm was rated at `η_forward` of the load it was holding** | The same fault as above met in the kind where it costs most. A worm stage rates contact on the wheel's torque, and built that as `max(T_in, T_back) · i · η_forward` — correct for the drive, and wrong for a load arriving at the wheel already, which has no forward efficiency to pay. Holding **400 N·m** on the shipped worm's output, the mesh was rated as though the wheel carried 247: **6813.6 MPa reported as 5803.9**, 17 % low, on the case a self-locking worm is chosen for. The gate builds the same wheel torque from either end — driven forward hard enough to deliver it, and back-driven by it with nothing coming the other way — and demands one answer, which is the *mechanism has no forward* rule in its plainest form |
| [rationale.md#direction-is-the-readers-not-the-mechanisms](rationale.md#direction-is-the-readers-not-the-mechanisms) | **A locked mesh delivered a torque to the shaft behind it** | The correction that stopped a self-locking worm's wheel reporting 2.2e307 N·m removed a division by the backward efficiency. The factor belongs there — on the member the load *leaves* by, not the one it arrives at — and dropping it altogether left the worm claiming **0.0150 N·m** on a shaft a locked mesh delivers nothing to. Driving forward, the output member carries the input's torque referred by the ratio and cut by the forward loss; being driven, the same construction with the roles swapped puts the backward loss on the worm. So the number a stage reports is now the number it hands to the stage before it, rather than that number before its own loss. The old test could not tell the two apart: it ran only on a self-locking fixture, where `η_backward` is zero and "times `η_backward`" and "times nought" agree. **Both sides of the threshold now**, which is what makes the factor a factor |
| [reference.md#contact-stress](reference.md#contact-stress) | **A zero force was refused where the limit is a closed form** | `elliptical_contact` treated no load as impossible input, alongside a negative one. The patch closes as `F^⅓` and the pressure with it, so the value at zero is the limit of the values approaching it — the expression merely happens to read `0/0` there. Refusing it cost a **worm stage its entire solve**: a train's operating torque may legitimately be nought, and asking one what it does at rest returned `NoContact`, which the reader is told means *the teeth never come into contact*. Every parallel-axis kind answered, because line contact reaches zero through a `sqrt` rather than a quotient. The same refusal was hiding a second thing: `gear-cli crossed` printed "the teeth never come into contact" for a helix split that self-locks, where the teeth touch perfectly well and simply transmit nothing — the row now reports its geometry and its `0.000 %` |
| [state.md](state.md) | **The golden script took whatever binary was on disk** | It built `gear-cli` only when there was none, so a corpus written after a change to the harness could be recorded from code that no longer existed — and a *newly added* case was invisible until something else forced a rebuild, at which point `--write` had already deleted nothing and recorded 22 files where there were 23. This is the stale-binary fault this log records, met inside the instrument written to catch it: **a check that finds its subject rather than building it is checking whatever ran last.** Cargo is incremental, so the fix costs an up-to-date tree nothing |
| [rationale.md#where-closed-form-is-impossible](rationale.md#where-closed-form-is-impossible) | **"The search is converged, not budgeted" was a comment about numbers nothing could raise** | Six tuned constants sat inside `maximise`'s loop with a paragraph beside them reporting what happens at fourteen times the work — the pair's answer not moving to eight decimals, the set's by 2e-7. Nothing could reproduce that: a claim *about* a budget cannot be checked while the budget is a `const` in the body. Made a value (`auto::Search`), and half the claim is false. A pair's search is converged over fourteen tooth pairs, worst 4.1e-7 in efficiency and under one resolution step in any shift. An epicyclic set's moves `η₀` by up to **2.4e-4** with the sun's shift **0.30 modules** away, and it moves **both ways** — at three of thirty sets the refined search returns a *worse* answer than the shipped budget did, so no budget is the repair. The diagnosis is the pair, which is the control: it is searched in the pair's own coordinates, the shift sum and the division, so its flat direction is an axis; the set is searched in two of its three raw shifts, so its walk slides along a curved bound instead of climbing. *A tuned constant with an argument beside it is a measurement nobody can repeat until the constant is an input* |
| [reference.md#efficiency-parallel-axes](reference.md#efficiency-parallel-axes) | **A search's opening resolution was deciding feasibility** | `auto::maximise` swept a fixed `±3` modules at half a module a step, which is a guess at where shifts live rather than a statement of it. Pin the shift *sum* — which is what giving a centre distance does — and the admissible interval in the remaining shift can be **a tenth of that step** and fall between two grid points, at which point the sweep collects nothing and the search reports *no admissible pair*. The stage then drops to its undercut floor, silently. A 9/37 pair asked to run at the **24.42 mm it had itself just recommended** came back at 97.289 % against the 97.706 % it offered unasked, at shifts whose own centre distance is 23.86 — not idempotent under its own output, which is a fault this log already records of a crank offset. The box is the members' own admissible interval now (`auto::searchable_shift`), bisected off the one rule that states it rather than re-derived beside it, so the sweep spans the answer by construction. The gate is the law rather than the figure: at the distance the free search chose, the given-distance answer must be the same answer |
| [reference.md#efficiency-parallel-axes](reference.md#efficiency-parallel-axes) | ...and where both peaks existed the search took the lower one | The same box, read for what it is worth on the shifts rather than on feasibility. At a fixed sum the efficiency against one member's shift is **bimodal**, and the trough sits exactly where the automatic addendum cap engages: capping shortens the tip, which shortens the path, which cuts sliding loss faster than the shift alone, so past the cap the efficiency rises again. Two regimes, two peaks, and a sweep at half a module a step lands in whichever it lands in — 1.2e-4 of efficiency and a pinion shift 0.20 modules away, on the shipped path at a plain centre-distance input |
| [reference.md#efficiency-parallel-axes](reference.md#efficiency-parallel-axes) | **The table for the crate's own optimiser had drifted, and no command could have caught it** | Every figure in the least-loss table was stale — 97.62 → **97.706 %** and `Σx` 1.20 → 1.67 on 9/37, and the whole *least shift* column, which still quoted the raw undercut minimum where `automatic_profile_shift` has clamped it at zero for as long as the toggle has existed: −0.65 against +0.4736. The prose with it: 1.6 points of efficiency given up where it is 0.14, a contact-ratio trade of 3.04 → 1.48 where it is 1.60 → 1.46, and "the optimum is **interior**", which is true of 17/43 and false of 9/37, where every improving direction describes a tooth that cannot be cut. **Nothing could have caught it**: no `gear-cli` command set `Optimisation::enabled`, so the crate's one search — whose answers these are — was outside the golden corpus entirely, exactly as `back_driving_torque` was. Confirmed stale rather than newly moved by building the previous commit and reading the same figures out of it. `gear-cli shifts` prints the table and the distance sweep beside it, and both are recorded |
| [state.md](state.md) | ...and the figure checker had the golden script's stale-binary fault too | `tools/check_figures.py` took `target/release/gear-cli` if it was there and built nothing. So a block tagged with a **newly added** command read as drifted while the document was right and the binary was old — the same fault as `check_golden.sh`'s, in the second of the two instruments written to catch drift, found within an hour of fixing the first. *An instrument that finds its subject rather than building it is measuring whatever ran last*, and it is worth grepping for the pattern rather than fixing the instance |
| [rationale.md#a-ring-is-asked-of-its-cutter-not-of-a-rack](rationale.md#a-ring-is-asked-of-its-cutter-not-of-a-rack) | **"A ring is not asked" was read as "a ring is asked nothing"** | `member_is_buildable` declines a rack's four questions of a ring, correctly, because its root and fillet are its shaper's. Nothing then asked it anything else — so an epicyclic set's shift search was free to walk past the point where the shaper stops leaving the space that shift asked for, and it did: **26 of 30 sets swept** returned a ring the cutter had capped, the shipped 13/25 choosing **2.35 modules** where its space caps at 1.94. Every efficiency reported for those is the efficiency of a part nobody makes. The fix is the question a ring *can* answer — did the tool leave the shape asked for — and it is free, because every caller has already cut the ring to get the mesh it is scoring. What is worth recording beside it is the *other* half: two of the four questions carry over **unchanged**, since a ring's space is generated the way a tooth is and takes the identical expression, so `admissible_profile_shift` already bounded it and there was never a second range to write. Reading a rack's bound onto a ring instead — which is what "give the ring a range" first meant — caps it near 1.2 modules and costs every set between 1e-5 and 4.5e-4. *The tool is the parameter; the member is not the branch* |
| [rationale.md#a-constraint-belongs-to-the-mesh-not-to-the-arrangement-around-it](rationale.md#a-constraint-belongs-to-the-mesh-not-to-the-arrangement-around-it) | **Three kinds each wrote out what to ask of a mesh, and answered it three ways** | The parallel pair asked whether its teeth bottom out — a tip reaching past the root circle it runs into — and **neither epicyclic kind did**. The pair and the set asked whether each member could be cut as asked; **a ring was asked by nobody**. Each gap was invisible from inside the kind that had it, because the kind that got it right was a different file. Measured, the sets do not currently violate the bottoming they never checked — 0.03 to 0.11 mm of clear air — so this is a latent gap rather than a live wrong number, and it is recorded for what it says about *where a constraint belongs*: to the mesh, which does not know what is turning around it, rather than to the arrangement. One place now (`auto::MeshTrial`), with the tool arriving as a parameter (`auto::Cut`) rather than as a branch, and `Mesh::bottom_clearance` one expression for both mesh kinds with the sign doing the work. Every stage's answers are unchanged to the bit, which is the point: the extraction moved no number and closed two holes |
| [rationale.md#where-closed-form-is-impossible](rationale.md#where-closed-form-is-impossible) | **A multi-start search that ran one start** | `auto::Search::budget` was a pool shared across every start, and a walk terminates on its own resolution after 80 to 608 evaluations — so on an epicyclic set the **first walk of six spent the entire pool of 220 and the other five never ran**. The third is the one that finds the better ridge. That is where the 3.2e-4 of `η₀` this log records as "the set's search is not converged" had been going, and it was never a matter of coordinates or of basins: more starting points changed nothing because there were no more starting points. A pool is also an order dependence — which starts run is decided by how dear the earlier ones happened to be. Per walk, the number is a ceiling nothing reaches instead of a share everything divides, and quadrupling it now moves **no answer by a bit**, which is the claim a guard can make and a truncation cannot. Found by logging what each walk cost, after two wrong diagnoses from reasoning: *the cheapest instrument is the one that prints what actually happened.* It costs a pair eight times what it was paying and buys it nothing — its answer is the same at a fifth of the guard — and the timing gate's ceilings went up with the reason written beside them, its multiplier coming down from ten to five so it did not go slack while the measurement grew |
| [state.md](state.md) | ...and the starts were deduplicated without being sorted | `Vec::dedup` removes *consecutive* duplicates. The starts arrive from two rules — the sweep's best few and its outermost — so the same grid point arrives twice and not next to itself, and one walk in six was spent re-walking a point already walked. Invisible while five of the six were being dropped for want of budget |
| [rationale.md#where-closed-form-is-impossible](rationale.md#where-closed-form-is-impossible) | **A walk that stopped before it started** | The walk's first step is a fraction of the sweep's spacing, and the spacing is a fraction of the *box*. Pin a centre distance and the box is a fraction of a module wide, at which point the first step falls **below the distance the walk stops at** — so `step > resolution` was false before the body ran, no step was ever taken, and the search's answer was the best of thirteen grid points. It is the narrow-box case that suffers, which is exactly the given-centre-distance path: on 9/37 at a shift sum of 0.56 the optimum sits four ten-thousandths above the undercut floor, between two grid points, and the grid cannot see it — **3.6e-5** of efficiency. Found by scanning the same admissible interval at the search's own stopping distance and comparing, an instrument sharing no step, no direction and no budget with the walk; the two agreed to 1e-8 wherever the box was wide and differed by four orders more wherever it was not, which is the shape that names the cause. *A tolerance compared against a quantity that scales with something else is a tolerance that means different things in different places.* |
| [rationale.md#and-the-one-thing-in-the-crate-that-is-none-of-the-above](rationale.md#and-the-one-thing-in-the-crate-that-is-none-of-the-above) | **"The sum is searched and the division solved" — the division was searched too** | Three places said the optimiser searched one dimension where it searches two: the rationale's inventory, the reference's efficiency section, and the search function's own heading. `contact::efficient_split` and `split_residual` are derived, tested and called by **nothing outside `contact.rs`'s own tests**, which a grep for their callers settles in a second and nobody had run — the claim survived because each document was checked against the *other documents*. Found while trying to extend the solve, which is the way a false claim about the code most often surfaces: by acting on it. And the derivation itself carries an unstated assumption that the shipped default breaks — `1/sin α_a` is `dξ/dr_a`, and turning it into a rate per unit *shift* wants `dr_a/dx`, which is `m` only while the addendum is a given number. `no_sharp_tip` is on by default and holds a capped tip where the tooth is `min_tip_width` wide: `dr_a/dx = 2r_a c/(2 tan α_a − w_min/r_a)` with `c = 2 tan α_n/z`, about **0.47 m** on a nine-tooth pinion and different on each member, so the factor stops cancelling. Measured against the closed form directly: the solver matches a brute scan to 1e-4 wherever the cap is idle and misses by a third of the interval wherever it is not. *A claim about what the code does is checked by grepping for callers, not by reading the sentence next to it* |
| [state.md](state.md) | **Five of the rating model's constants could be perturbed and nothing failed** | Mutation, run against the suite one constant at a time: `inv α` off by two parts in ten thousand fails **fifty** tests; the loss integral off by a tenth of a percent fails six; `K_f`'s `H` and `L`, ISO's `Y_S`, the 30° tangent angle and the reversed-bending fraction fail **none**. The geometry is gated to the bit and the strength model is not gated at all — the shape of a suite grown one geometric learning at a time. Four of the five are caught by `tools/check_golden.sh`, which is the change detector doing exactly its job; what was wrong was the advice, since `CLAUDE.md`'s "before pushing, run four" did not include it. *A `nextest` run is not evidence that the strength model is the one that was there yesterday*, and it now says so |
| [rationale.md#load-sharing-is-offered-rather-than-assumed](rationale.md#load-sharing-is-offered-rather-than-assumed) | **The ramp's two constants were guarded by a test written in terms of them** | `the_load_share_is_continuous_and_unchanged_below_two` is thorough about *shape* — continuity, an exact identity below `ε = 2`, the size of the step it replaced — and every one of those assertions names `RAMP_MIN` and `RAMP_MAX` on both sides, so moving either moves the comparison with it. This log's own "a gate on a ratio cannot see a scale error", recurring. Nothing else could see them either, and for a reason worth keeping: below `ε = 2` the governing point *is* the single-pair boundary, where the share is exactly one, so the ramp reaches no answer the tool reports. Perturbing `RAMP_MIN` by two hundredths left 558 tests, 26 golden files and every documented figure unchanged. The two ends are pinned as figures now — a canary, which is the right instrument for a number that is disclosed as uncalibrated and that nothing derives |
| [state.md](state.md) | **The harness turned three of a gear's eleven controls** | `gear-cli sweep` is the parameter grid, and it swept `z`, `x`, `α` and `β` while leaving the eccentric feature, the tool's tip round and the thickness modification at their defaults; across the whole harness, `reversed_bending`, `load_sharing`, `no_undercut`, `no_sharp_tip`, `rim_thickness` and `material_overrides` were never once switched. So the constants behind them were outside the change detector by construction — the fault this log records of a back-driving load and of the optimiser, met a third and fourth time. *An opt-in the harness never switches on is a path the detector cannot see.* `sweep` turns every gear axis now (5,916 cases to 94,656, in 59 ms) and `gear-cli train toggles` turns every stage one |
| [reference.md#export-and-import](reference.md#export-and-import) | **The test named for the property the module exists to provide measured something else** | `worst_deviation` returned the longest **chord**, with the real computation abandoned mid-line — `let _ = mid_r;` sits in the committed source where the distance to the profile was going to be worked out. So nothing checked that a drawn outline is within the chord tolerance it was given, which is the module's whole promise and what a CAD import relies on. Everything written on top was **relative** — tighter tolerance gives shorter chords, more vertices — and a relative test cannot see a scale move: `DEFAULT_CHORD_TOLERANCE` doubled and `MAX_SUBDIVISION_DEPTH` cut from 14 to 10 each left all 558 tests and all 27 golden files silent. Measured properly against the gear's own dense sampler, the outline is inside 3× its tolerance and **converges on it** — 2.7× at 1e-2, 1.8× at 1e-3, 0.99× at 1e-4, worst on an undercut tooth whose flank is re-entrant, because subdivision stops on a span's *midpoint* sagitta and a curve's worst deviation exceeds it |
| [state.md](state.md) | ...and the first version of that measurement measured the reference | Written against a fixed sampling density it reported the outline straying by **three times** its tolerance, and the shape of the answer was the giveaway: the deviation *rose* as the tolerance tightened, which is impossible for a convergent scheme. The polyline had overtaken the curve it was being compared against, so what was being measured was the reference's own coarseness. It is derived from the outline's own vertex count now, ten times denser than whatever it is judging. *A reference is only a reference while it is finer than its subject*, and a fixed density is not a promise of that — the same trap as a sweep whose resolution decides the answer |
| [reference.md#load-cases](reference.md#load-cases) | **A probe width and a default face width happened to be the same number** | An epicyclic set rates once at `PROBE` and scales to the width each mesh carries — bending inversely with it, contact with its square root. `PROBE` is 10.0 and `StageGear`'s default face width is 10.0, so every shipped case scales by exactly one and the exponent could have been anything: changing it to 0.51 left all 558 tests and all 27 golden files unchanged. *Two unrelated numbers that happen to be equal will hide whatever lies between them.* Gated by asking a set for a stress at widths well either side of the probe and comparing against a direct evaluation there |
| [rationale.md#no-engineering-calculation-in-typescript](rationale.md#no-engineering-calculation-in-typescript) | **The boundary has engineering numbers of its own, and nothing could see them** | Rule 1 says that if a number appears in the UI, Rust computed it — *a default is one of those numbers*. Most of the panel's defaults are `gear-core`'s and the golden corpus sees them, because `gear-cli` builds its stages from the same `Default`. A handful are **the boundary's own**: the gear tab's tooth count, the pin diameter, the eccentric throw, the face width a fresh panel seeds, the speed and torque a fresh train carries. Perturbing any of them left **every test, the whole corpus and the binding check silent** — they reach a designer without passing anything that could notice them moving. Pinned as figures now, which is what a canary is for: none is derived, and the only property they have is that nobody changes them by accident |
| [rationale.md#no-engineering-calculation-in-typescript](rationale.md#no-engineering-calculation-in-typescript) | ...and writing that canary found the panel answering one question three ways | The rule beside the code is that a designer opening a stage should see *the width the rating asks for*, seeded at 5 mm so the field has something to fall back to. It reached a parallel pair and an epicyclic set. A **hula stage opened at a fixed 10 mm** — not automatic at all, so a fresh panel showed a width nobody had chosen and no rating had sized — and a worm's members were automatic but seeded at ten. Three answers to one question, decided by which stage kind a designer happened to pick. Found by writing a test that walks *every member of every kind the panel offers* rather than the two the rule had reached, which is the same walk `StageResult::members()` exists for |
| [state.md](state.md) | **A note declared unfirable had never been fired at** | `stage.ring_addendum_clamped` sat in `UNFIRED` on the reading that a set solves its ring's addendum and so does not hand it one that cannot work. It fires on **441 of the 1,482** sets a sweep of tooth counts, addenda, shifts, cutters and thickness modifications can solve. The sweep *did* carry five cases aimed at it, and every one failed with *the teeth never come into contact* inside an `if let Ok(…)` that said nothing — so "looked for and not found" was true of the sentence and false of the code. Two faults in the five: they **raised** the ring's addendum where a ring's tip is `r − m(h_a − x)` and reaches its base circle on a *short* one against a negative shift; and they left the ring's tooth count at `StageGear`'s own 17 while setting the sun's and the planet's, which is a ring that does not close the set it is in. *A case that cannot solve is not a case*, and an `if let Ok` around one is how it stays that way quietly. The other entry's evidence was re-run rather than restated — 71,750 combinations against the original 11,000, and it still never fires |
| [reference.md#the-centre-distance-a-pair-runs-at](reference.md#the-centre-distance-a-pair-runs-at) | **A conditional that decided nothing, explaining a rule it did not enforce** | `SpurStage::clearance_taken` returned the stage's clearance "wherever anything is free to absorb it" and zero otherwise, with four paragraphs on which case is which. **Every caller already ran where its condition held**: the centre distance reads it only when it is automatic, and the shift search reads it only when the optimiser is on, so the arm returning zero was unreachable in effect. Replacing the whole thing with the field moved no test and no recorded figure. The rule it describes is real and is enforced by *where the clearance is read*, not by what this returned — so it is stated there, and a method that suggested there were two answers here is gone. Found by mutating the five modules `AUDIT.md` records as carrying no inline tests, to see which are covered elsewhere: four are, by the corpus or by another module's tests, and this branch was the one thing nothing anywhere could see |
| [state.md](state.md) | ...and the reason nothing could see it was a sixth opt-in path | A spur stage with a centre distance **given** and the optimiser **off** — the plainest thing a designer does — was not in the golden corpus. Every recorded case either left the distance automatic or turned the optimiser on. `gear-cli shifts` prints that combination now, beside the optimised one, which is the trade the reference section is about anyway. That is the sixth time this audit has found a path outside the change detector because nothing switched the toggle that reaches it |
| [state.md](state.md) | **"The inline tests never had the integration tests' consolidation"** | Recorded as a finding on the reading that `tests/` was pulled onto one shared grid and `src/`'s `#[cfg(test)]` blocks were left as the one-off fixtures they grew from. Measured instead of read, and the premise is **largely false**: 32 mutations firing 122 times across 101 distinct tests, only 19 of them firing on more than one mutation, and every "subsumed" pair turning out to be an unrelated broad canary — so there is no measurable redundancy to consolidate, and the nested sweeps each turn the axes their own law needs. What the measurement *did* find, by clustering the tests by **subject** rather than by file, is one law stated in two places at two standards: nine thickness-modification tests across five files, of which `ring.rs`'s sweeps three counts by three shifts by four values of `k`, checks the cutter's plunge and carves out the one case the rule bends, while `tests/geometry_laws.rs`'s asserted the same rule on **one default gear**. *A finding about a whole class of tests is a hypothesis until something counts.* |
| [reference.md#tooth-thickness-and-its-equivalent-shift](reference.md#tooth-thickness-and-its-equivalent-shift) | ...and levelling the weaker half up found where the law bends | Widened to the shared grid by five values of `k` — 540 gears where there had been three — and the first sweep failed: a three-tooth pinion at 14.5° and `x = −0.5` moves its tip radius from 0.2082 to 0.1726 mm at `k = 0.6`. Not a fault. The tooth is **severed**, so its tip is set by the shape that survives rather than by the addendum asked for, and how much survives is a function of the thickness — which is the exact mirror of the ring's space-closed carve-out that the better half of this law already carried. The law splits where the wider sweep says it splits: `r` and `rb` are exact always, since the tooth form does not enter `m·z/2` or `r·cos α_t`; `ra` and `rf` are exact unless the profile truncated them; and `st` must have moved, or the test asserts that a control does nothing. Gated by mutation — giving `rb` a `k`-and-helix dependence is caught here now, and could not have been caught before, because the old fixture never turned the helix axis and the factor is one at `β = 0` |
| [reference.md#internal-gears](reference.md#internal-gears) | **A designer was told whether their teeth foul according to which stage kind they had picked** | The three ways an internal pair's teeth can foul — the pinion's tip past where the ring's flank ends, the ring's tip below where the pinion's does, and the tips fouling **away from the line of action** where their circles cross — were four fields on a *hula stage's* mesh row, computed in that stage's file and drawn in that stage's form. An epicyclic set has the same internal mesh in it and reported nothing. This log's "a constraint belongs to the mesh" a second time, on the *reporting* side: `auto::MeshTrial` had already put what is **asked** of a mesh in one place, and what the asking found was still filed under the arrangement. One `train::TipRoom` on `MeshReport` now, `None` on an external mesh because the question does not arise there rather than because the answer is three falses, and the search reads the same value as its fourth refusal — the reader and the constraint cannot disagree. `StageResult::meshes()` arrives beside `members()` for the reason `members()` exists: *a walk that names the kinds is a walk that forgets one*, and the panel's shared `meshRows` snippet draws the row once for every kind that has a ring in it |
| [reference.md#internal-gears](reference.md#internal-gears) | ...and the shipped epicyclic set fails one of the three | `ring::mesh_with` has known since it existed that a **full-depth internal pair interferes**: the ring's tip can only touch the pinion's involute while `√(r_a2² − r_b2²) ≥ a sin α_w`, a 60-tooth ring on a 20-tooth pinion misses it by 0.009 mm, and `ring.rs` carries a test saying so across every pinion from 20 to 40 teeth. Nothing ever put the question to a **set**, whose default ring is full depth at zero shift — so the shipped 24/18/60 has involute interference on its planet-ring mesh and had never said a word about it. It says so now, and `gear-cli planetstage` prints it, which is what puts it in the change detector. The remedy is the ring's addendum, which is an input: shortening it to 0.75 clears the same set, and that is the second half of the gate, because a canary that only watches the shipped set interfere would pass if everything interfered for a new reason. **Measured before it was wired**: over thirty sets with the ring shift pinned, refusing a fouled mesh moves two answers, both by under 5e-5 of `η₀`; across the whole corpus one number moves, a shift division by 0.002 modules at the same efficiency to four decimals |

---

## Process notes

- **A green local test run does not imply a green build.** Twice, the working
  tree held something a fresh checkout does not: a data file the source filter
  stripped, and generated bindings that are gitignored. `git add` before
  `nix build`; flakes only see tracked files.
- **Typechecking is not running unless you run it.** `cd web && npm run check`.
- **Run the app.** Two real defects in one session were found on screen and by
  nothing else. `--dump-dom` and `--screenshot` both run the JS, so they prove
  the app mounted and computed.
- **When a refactor must not change what a user sees, diff what a user sees.**
  The gate for pulling 185 strings into a catalogue was the rendered DOM, byte
  for byte. It caught a reactivity bug immediately, which no typecheck would
  have.
- **A mutation that does not compile is not a mutation.** The harness edited a
  constant, rebuilt, and grepped the run for `FAIL [`. A mutation that names a
  binding out of scope fails to *build*, so there is no `FAIL` line and the
  harness records the strongest result it has — "no test caught it", which reads
  as a gap in the suite. It builds first and records `DOES-NOT-COMPILE`
  separately now. An instrument whose failure mode is indistinguishable from its
  most interesting finding will hand you that finding.
- **A scratch verification is not a verification once the directory is gone.**
  The crossed-axis derivation was checked in numpy before a line of Rust was
  written, and those scripts lived in a temporary directory while the design
  document cited their results as settled. Promoting them to `tools/` found two
  faults in the checks themselves.
